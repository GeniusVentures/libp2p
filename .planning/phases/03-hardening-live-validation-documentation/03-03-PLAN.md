---
phase: 03-hardening-live-validation-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - example/05-private-network/CMakeLists.txt
  - example/05-private-network/private_network_example.cpp
  - example/05-private-network/README.md
  - example/07-connection-gater/CMakeLists.txt
  - example/07-connection-gater/connection_gater_example.cpp
  - example/07-connection-gater/README.md
  - example/06-private-network-gater/CMakeLists.txt
  - example/06-private-network-gater/private_network_gater_example.cpp
  - example/06-private-network-gater/README.md
  - example/CMakeLists.txt
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03]
user_setup: []

must_haves:
  truths:
    - "Running example/05-private-network's binary demonstrates a Host configured with a PSK via usePrivateNetwork(...) (DOCS-01)"
    - "Running example/07-connection-gater's binary demonstrates a Host configured with a custom ConnectionGater via useConnectionGater<T>() that observably rejects a denylisted peer (DOCS-02)"
    - "Running example/06-private-network-gater's binary demonstrates a Host with BOTH usePrivateNetwork and useConnectionGater composed, and the gater denies a peer even though that peer would present a valid PSK (DOCS-03, per D-12: runnable code, not a narrated pseudocode walkthrough, and independent of the Phase 3 live two-node test)"
  artifacts:
    - example/05-private-network/CMakeLists.txt
    - example/05-private-network/private_network_example.cpp
    - example/05-private-network/README.md
    - example/07-connection-gater/CMakeLists.txt
    - example/07-connection-gater/connection_gater_example.cpp
    - example/07-connection-gater/README.md
    - example/06-private-network-gater/CMakeLists.txt
    - example/06-private-network-gater/private_network_gater_example.cpp
    - example/06-private-network-gater/README.md
  key_links:
    - "example/CMakeLists.txt add_subdirectory(...) for 05-private-network, 07-connection-gater, 06-private-network-gater (in that order, per Pitfall 4's confirmed numbering resolution) wires all three into the default example build"
---

<objective>
Author three new, standalone, single-topic example directories -- each a runnable binary, not narrated prose -- covering PSK configuration (DOCS-01), custom ConnectionGater registration (DOCS-02), and the worked "valid PSK, gater-denied peer" complementary-layers demonstration (DOCS-03). Follows this repo's existing "code is the documentation" convention (example/01-echo, 02-kademlia, 03-gossip) exactly, per D-08/D-09.

Purpose: DOCS-01/02/03 / ROADMAP Phase 3 success criteria #3, #4, #5.
Output: example/05-private-network/, example/07-connection-gater/, example/06-private-network-gater/ (each: CMakeLists.txt + one .cpp + README.md), plus example/CMakeLists.txt registering all three.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hardening-live-validation-documentation/03-CONTEXT.md
@.planning/phases/03-hardening-live-validation-documentation/03-RESEARCH.md
@.planning/phases/03-hardening-live-validation-documentation/03-PATTERNS.md

@example/01-echo/CMakeLists.txt
@example/01-echo/libp2p_echo_server.cpp
@example/01-echo/README.md
@example/CMakeLists.txt
@include/libp2p/injector/network_injector.hpp
@include/libp2p/network/connection_gater.hpp
@include/libp2p/network/impl/permissive_connection_gater.hpp
@test/libp2p/security/pnet/pnet_injector_test.cpp
@test/libp2p/injector/host_injector_test.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: example/05-private-network/ -- PSK configuration (DOCS-01)</name>
  <files>example/05-private-network/CMakeLists.txt, example/05-private-network/private_network_example.cpp, example/05-private-network/README.md</files>
  <read_first>
    example/01-echo/CMakeLists.txt (add_executable + target_link_libraries pattern)
    example/01-echo/libp2p_echo_server.cpp (full structure: logger_config YAML, soralog::LoggingSystem/ConfiguratorFromYAML bootstrap, TRACE_DEBUG env check, injector construction, setProtocolHandler, io_context->post(listen+start), io_context->run())
    example/01-echo/README.md (per-example README structure)
    include/libp2p/injector/network_injector.hpp lines 258-327 (usePrivateNetwork doc comment and both overloads, eager PskValidationError throw semantics)
    test/libp2p/security/pnet/pnet_injector_test.cpp lines 36-39 (kValidSwarmKey literal, reused byte-identical here)
  </read_first>
  <action>
    Create `example/05-private-network/CMakeLists.txt`: `add_executable(libp2p_private_network_example private_network_example.cpp)` plus `target_link_libraries(libp2p_private_network_example Boost::Boost.DI p2p_basic_host p2p_default_network p2p_pnet p2p_pnet_upgrader p2p_peer_repository p2p_inmem_address_repository p2p_inmem_key_repository p2p_inmem_protocol_repository p2p_protocol_echo p2p_literals Boost::date_time Boost::regex ${WIN_CRYPT_LIBRARY})` -- the exact verified library set from 03-PATTERNS.md's "example/05-private-network/" section.

    Create `example/05-private-network/private_network_example.cpp`: model directly on `example/01-echo/libp2p_echo_server.cpp`'s structure (the same `logger_config` YAML string, `soralog::LoggingSystem`/`ConfiguratorFromYAML` bootstrap, `TRACE_DEBUG` env var check) but simplified to ONE server-only `main()` -- no client/insecure-mode branching, since this example has exactly one topic (PSK configuration), per D-10's one-topic-per-example-dir rule. Build the injector via `libp2p::injector::makeHostInjector(libp2p::injector::usePrivateNetwork("/key/swarm/psk/1.0.0/\n/base16/000102030405060708090a0b0c0d0e0f101112131415161718191a1b1c1d1e1f\n"))` -- reuse the exact swarm-key text already used as `pnet_injector_test.cpp`'s `kValidSwarmKey`, so the documented example key is byte-identical to one already verified elsewhere in the test suite, not an untested ad hoc literal. Register an Echo protocol handler exactly like `libp2p_echo_server.cpp` (`libp2p::protocol::Echo echo{...}`, `setProtocolHandler`). Listen on `/ip4/127.0.0.1/tcp/40530` -- a DIFFERENT port from `01-echo`'s 40010, to avoid collision if both examples run simultaneously during manual testing. `io_context->post([...]{ host->listen(ma); host->start(); std::cout << ...peer id... << " -- requires a matching PSK to connect\n"; })`, then `io_context->run()`.

    Create `example/05-private-network/README.md`: explain (a) what a private network / PSK is (the `/key/swarm/psk/1.0.0/` swarm-key framing, XSalsa20, 256-bit key, per the libp2p pnet spec), (b) how to generate/obtain your own swarm.key text and substitute it for the hardcoded example key before real use -- the example key is a publicly-known test vector, never a real secret, (c) build/run instructions mirroring `example/01-echo/README.md`'s "Build C++ targets... Start... Watch..." structure, (d) expected behavior: a peer WITHOUT the same PSK cannot open a stream to this server, cross-referencing `../06-private-network-gater/README.md` for the combined worked example (created by Task 3 of this same plan).
  </action>
  <acceptance_criteria>
    `cmake -S . -B build -DEXAMPLES=ON` reconfigures cleanly (the existing build/ cache has EXAMPLES:BOOL=OFF, confirmed against build/CMakeCache.txt -- must be flipped ON to build examples).
    `cmake --build build --config Debug --target libp2p_private_network_example -- -m` succeeds.
    Running `build/example/05-private-network/Debug/libp2p_private_network_example.exe` for a few seconds prints a listening address and peer ID to stdout with no unhandled exception (bounded manual/timed run is acceptable -- this is a long-running server binary, not a test).
  </acceptance_criteria>
  <verify>
    <automated>cmake -S . -B build -DEXAMPLES=ON &amp;&amp; cmake --build build --config Debug --target libp2p_private_network_example -- -m</automated>
  </verify>
  <done>example/05-private-network/ exists with all 3 files, builds cleanly, demonstrates usePrivateNetwork(...) as the sole new concept per D-10.</done>
</task>

<task type="auto">
  <name>Task 2: example/07-connection-gater/ -- custom ConnectionGater registration (DOCS-02)</name>
  <files>example/07-connection-gater/CMakeLists.txt, example/07-connection-gater/connection_gater_example.cpp, example/07-connection-gater/README.md</files>
  <read_first>
    example/01-echo/CMakeLists.txt and example/01-echo/libp2p_echo_server.cpp (structural template, as in Task 1)
    include/libp2p/network/connection_gater.hpp (the 5-hook interface: interceptPeerDial/interceptAddrDial/interceptAccept/interceptSecured/interceptUpgraded, each `outcome::result&lt;void&gt;`)
    include/libp2p/network/impl/permissive_connection_gater.hpp (default-gater reference for the 4 hooks this example passes through unchanged)
    include/libp2p/injector/network_injector.hpp lines 238-256 (useConnectionGater&lt;GaterImpl&gt;() doc comment and body)
    test/libp2p/injector/host_injector_test.cpp (CustomAdaptors test's call-shape precedent for composing injector modules)
  </read_first>
  <action>
    Create `example/07-connection-gater/CMakeLists.txt`: `add_executable(libp2p_connection_gater_example connection_gater_example.cpp)` plus `target_link_libraries(libp2p_connection_gater_example Boost::Boost.DI p2p_basic_host p2p_default_network p2p_peer_repository p2p_inmem_address_repository p2p_inmem_key_repository p2p_inmem_protocol_repository p2p_protocol_echo p2p_literals Boost::date_time Boost::regex ${WIN_CRYPT_LIBRARY})` -- mirrors `example/01-echo/CMakeLists.txt`'s set exactly; no pnet libraries needed since `ConnectionGater` and `PermissiveConnectionGater` are already part of `p2p_default_network`'s sources.

    Create `example/07-connection-gater/connection_gater_example.cpp`: define `struct DenylistGater : public libp2p::network::ConnectionGater` implementing all 5 hooks. Four of them return `libp2p::outcome::success()` unconditionally (fully permissive, matching `PermissiveConnectionGater`'s default behavior for everything not explicitly denied). `interceptPeerDial` returns a rejection (`libp2p::outcome::failure(std::make_error_code(std::errc::permission_denied))` -- no new `ConnectionGaterError` value; this is example/integrator code, not library code) when the target `peer::PeerId` equals one hardcoded `kDeniedPeerId` constant (any well-formed base58 peer-id string literal decoded via `peer::PeerId::fromBase58(...)`, e.g. reuse the literal already used elsewhere in this repo's tests: `"QmbLHAnMoJPWSCR5Zhtx6BHJX9KiKNN6tpvbUcqanj75Nb"` from `dialer_test.cpp`'s bootstrap-peer-ID test), else `outcome::success()`. Build the injector via `libp2p::injector::makeHostInjector(libp2p::injector::useConnectionGater&lt;DenylistGater&gt;())`. After `host->start()`, construct a `peer::PeerInfo` for `kDeniedPeerId` at a throwaway loopback address (e.g. `/ip4/127.0.0.1/tcp/1`) and call `host->newStream(deniedInfo, {echo.getProtocolId()}, [](auto &&r){ std::cout << (r ? "UNEXPECTED: stream opened\n" : "denylisted peer correctly rejected by the custom gater\n"); })` so running the binary produces directly observable console proof the custom gater is wired and active. Listen the server itself on `/ip4/127.0.0.1/tcp/40531`.

    Create `example/07-connection-gater/README.md`: explain the `ConnectionGater` interface's 5 hooks, how `useConnectionGater&lt;T&gt;()` replaces the default `PermissiveConnectionGater` via one DI binding with zero source changes to `Dialer`/`TcpListener`/`UpgraderSession`, build/run instructions mirroring `01-echo/README.md`'s structure, and the observed denial output.
  </action>
  <acceptance_criteria>
    `cmake --build build --config Debug --target libp2p_connection_gater_example -- -m` succeeds (build/ already reconfigured with -DEXAMPLES=ON by Task 1).
    Running `build/example/07-connection-gater/Debug/libp2p_connection_gater_example.exe` prints "denylisted peer correctly rejected by the custom gater" (not "UNEXPECTED") to stdout.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build --config Debug --target libp2p_connection_gater_example -- -m</automated>
  </verify>
  <done>example/07-connection-gater/ exists with all 3 files; DenylistGater implements the full ConnectionGater interface; the example is runnable and self-demonstrates the rejection via console output.</done>
</task>

<task type="auto">
  <name>Task 3: example/06-private-network-gater/ -- complementary-layers worked example (DOCS-03) + example/CMakeLists.txt registration</name>
  <files>example/06-private-network-gater/CMakeLists.txt, example/06-private-network-gater/private_network_gater_example.cpp, example/06-private-network-gater/README.md, example/CMakeLists.txt</files>
  <read_first>
    example/07-connection-gater/connection_gater_example.cpp (Task 2's output -- the DenylistGater pattern being reused/adapted, standalone copy per D-09/D-10)
    example/05-private-network/private_network_example.cpp (Task 1's output -- the usePrivateNetwork pattern being reused)
    example/CMakeLists.txt (current 4-line add_subdirectory list, to be edited not rewritten)
    .planning/phases/03-hardening-live-validation-documentation/03-RESEARCH.md ("Pitfall 4: example/ directory numbering collision" -- confirms 07-connection-gater is the only non-colliding slot for the gater-only example; 05 and 06 are locked verbatim by D-10/D-11)
  </read_first>
  <action>
    Create `example/06-private-network-gater/CMakeLists.txt`: `add_executable(libp2p_private_network_gater_example private_network_gater_example.cpp)` plus `target_link_libraries(...)` combining BOTH Task 1 and Task 2's library sets (adds `p2p_pnet`/`p2p_pnet_upgrader` on top of the plain-gater set: `Boost::Boost.DI p2p_basic_host p2p_default_network p2p_pnet p2p_pnet_upgrader p2p_peer_repository p2p_inmem_address_repository p2p_inmem_key_repository p2p_inmem_protocol_repository p2p_protocol_echo p2p_literals Boost::date_time Boost::regex ${WIN_CRYPT_LIBRARY}`).

    Create `example/06-private-network-gater/private_network_gater_example.cpp`: define the SAME shape of `DenylistGater` as Task 2 -- a self-contained copy in this file, since example directories don't share a library target, matching D-09/D-10's standalone-per-topic convention. Build the injector via the 2-module composition verified in 03-RESEARCH.md's "DI injector call shapes" section: `libp2p::injector::makeHostInjector(libp2p::injector::usePrivateNetwork("/key/swarm/psk/1.0.0/\n/base16/000102030405060708090a0b0c0d0e0f101112131415161718191a1b1c1d1e1f\n"), libp2p::injector::useConnectionGater&lt;DenylistGater&gt;())`. Listen on `/ip4/127.0.0.1/tcp/40532`. After `host->start()`, run the SAME self-dial-to-`kDeniedPeerId` demonstration as Task 2, but change the printed message to make the complementary-layers point explicit, e.g.: "peer holds the correct PSK for this private network but is still denied by the gater -- PSK proves network membership, the gater proves peer-level authorization; they are independent, non-redundant checks."

    Create `example/06-private-network-gater/README.md`: this is DOCS-03's primary artifact. Explicitly walk through why PSK alone and the gater alone are each necessary but not sufficient on their own: a valid-PSK peer can still be gater-denied (this example proves it); a gater-allowed peer without the PSK can never even reach the point where the gater is consulted, since `PnetProtectedConnection` sits below multiselect while the gater hooks fire later in the upgrade pipeline. Reference `../05-private-network/README.md` and `../07-connection-gater/README.md` for the two single-layer examples this one composes. Include build/run instructions.

    Edit `example/CMakeLists.txt` (use Edit, not Write -- this is an existing file) to append exactly 3 lines after the existing `add_subdirectory(04-dnstxt)` line, in this exact order per the confirmed numbering resolution: `add_subdirectory(05-private-network)`, `add_subdirectory(07-connection-gater)`, `add_subdirectory(06-private-network-gater)`.
  </action>
  <acceptance_criteria>
    `cmake -S . -B build -DEXAMPLES=ON` then `cmake --build build --config Debug --target libp2p_private_network_gater_example -- -m` succeeds.
    Running `build/example/06-private-network-gater/Debug/libp2p_private_network_gater_example.exe` prints the complementary-layers message.
    `example/CMakeLists.txt` contains exactly 7 `add_subdirectory(...)` lines total (4 pre-existing + 3 new) in the order: 01-echo, 02-kademlia, 03-gossip, 04-dnstxt, 05-private-network, 07-connection-gater, 06-private-network-gater.
    A full `cmake --build build --config Debug -- -m` (all targets, no --target filter) succeeds with EXAMPLES=ON, proving none of the 3 new example targets broke the existing 4.
  </acceptance_criteria>
  <verify>
    <automated>cmake -S . -B build -DEXAMPLES=ON &amp;&amp; cmake --build build --config Debug --target libp2p_private_network_gater_example -- -m</automated>
  </verify>
  <done>All 3 new example directories exist, build, and are registered in example/CMakeLists.txt in the confirmed order; DOCS-01/02/03 are each satisfied by runnable, self-demonstrating code with a per-directory README (D-08/D-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| example code copy-pasted by an integrator into a real deployment | The example key/gater logic is illustrative, not production-hardened; an integrator who copies it verbatim without substitution introduces risk into their own deployment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Information Disclosure | example swarm-key literal reused verbatim in a real deployment by an integrator | medium | mitigate | README.md for 05-private-network and 06-private-network-gater explicitly instructs replacing the hardcoded example key before any real use; the key is the same publicly-known test vector already used in pnet_injector_test.cpp, never a real secret |
| T-03-07 | Elevation of Privilege | integrator misunderstanding PSK-only or gater-only as sufficient defense-in-depth alone | medium | mitigate | example/06-private-network-gater/ and its README is the direct mitigation -- a worked demonstration that a valid-PSK peer is still denied by the gater, proving neither layer alone is redundant with the other |
</threat_model>

<verification>
All 3 example directories build under `EXAMPLES=ON`; each binary is independently runnable and prints observable proof of the DI configuration it demonstrates; example/CMakeLists.txt correctly registers all 3 new subdirectories alongside the 4 pre-existing ones.
</verification>

<success_criteria>
DOCS-01, DOCS-02, and DOCS-03 are each satisfied by runnable code (not narrated markdown) with a per-directory README, matching this repo's existing example/ convention exactly.
</success_criteria>

<output>
Create `.planning/phases/03-hardening-live-validation-documentation/03-03-SUMMARY.md` when done
</output>
