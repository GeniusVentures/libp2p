---
phase: 03-hardening-live-validation-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/libp2p/network/dialer_test.cpp
  - test/libp2p/transport/tcp/tcp_listener_test.cpp
  - test/libp2p/transport/upgrader_session_test.cpp
  - test/libp2p/security/pnet/pnet_protected_connection_test.cpp
autonomous: true
requirements: [TEST-04]
user_setup: []

must_haves:
  truths:
    - "interceptPeerDial, interceptAddrDial (non-relay), interceptSecured, and interceptUpgraded rejection paths, plus PnetProtectedConnection read/write completions, only invoke their downstream callback after the scheduler backend is explicitly drained -- never synchronously within the triggering call's own stack frame (per D-05/D-06/D-07)"
    - "interceptAccept's gater check and rejection handling are proven to run only from within the already-scheduler-deferred accept block, never from inside async_accept's own completion frame"
  artifacts:
    - test/libp2p/network/dialer_test.cpp
    - test/libp2p/transport/tcp/tcp_listener_test.cpp
    - test/libp2p/transport/upgrader_session_test.cpp
    - test/libp2p/security/pnet/pnet_protected_connection_test.cpp
  key_links:
    - "A single ReentrancyGuard/Scope RAII idiom is reused verbatim across all 4 files, matching RESEARCH.md's verified call-site table exactly (no new mocking technique invented)"
---

<objective>
Extend four EXISTING unit-test files with reentrancy-regression coverage for every gater-hook and pnet call site that today defers via `scheduler_->schedule(...)` — proving deferred dispatch, not synchronous inline invocation, actually holds under a worst-case synchronously-completing collaborator. No production code is modified; this is pure regression-test authoring against already-implemented, already-unit-tested Phase 1/2 code.

Purpose: TEST-04 / ROADMAP Phase 3 success criterion #2 — guard the exact mitigation (scheduler-deferred callbacks) that prevents a reentrant gater/pnet completion from corrupting in-flight dial/upgrade/connection state.
Output: 6 new passing TEST_F/TEST cases across dialer_test.cpp (+2), tcp_listener_test.cpp (+1), upgrader_session_test.cpp (+2), pnet_protected_connection_test.cpp (+2) -- 7 total, all existing tests in these files continue to pass.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hardening-live-validation-documentation/03-CONTEXT.md
@.planning/phases/03-hardening-live-validation-documentation/03-RESEARCH.md
@.planning/phases/03-hardening-live-validation-documentation/03-PATTERNS.md

@test/libp2p/network/dialer_test.cpp
@test/libp2p/transport/tcp/tcp_listener_test.cpp
@test/libp2p/transport/upgrader_session_test.cpp
@test/libp2p/security/pnet/pnet_protected_connection_test.cpp
@test/testutil/gmock_actions.hpp
@src/network/impl/dialer_impl.cpp
@src/transport/impl/upgrader_session.cpp
@src/transport/tcp/tcp_listener.cpp
@src/security/pnet/pnet_protected_connection.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: dialer_test.cpp + tcp_listener_test.cpp -- interceptPeerDial/interceptAddrDial/interceptAccept reentrancy</name>
  <files>test/libp2p/network/dialer_test.cpp, test/libp2p/transport/tcp/tcp_listener_test.cpp</files>
  <read_first>
    test/libp2p/network/dialer_test.cpp (existing fixture; DialRejectedByPeerDialGater and DialRejectedByAddrDialGater are the base cases being extended)
    src/network/impl/dialer_impl.cpp lines 62-77 (interceptPeerDial rejection: scheduler_->schedule([cb, err]{ cb(err); })) and lines 285-298 (non-relay interceptAddrDial rejection: scheduler_->schedule([wp, peer_id]{ rotate(peer_id); }))
    test/libp2p/transport/tcp/tcp_listener_test.cpp (existing fixture; AcceptRejectedByGaterClosesConnectionWithoutUpgrading is the base case being extended)
    src/transport/tcp/tcp_listener.cpp lines 138-204 (doAccept -- the SINGLE scheduler_->schedule wrapping both the interceptAccept call and its rejection handling; no additional nested schedule at the rejection point)
    .planning/phases/03-hardening-live-validation-documentation/03-PATTERNS.md ("Re-entry guard scaffolding" section under the dialer_reentrancy_test entry)
  </read_first>
  <action>
    In `dialer_test.cpp`, add a file-scope `ReentrancyGuard` struct with a nested RAII `Scope` (copy verbatim from `03-PATTERNS.md`'s "Re-entry guard scaffolding" block): a `bool inside` member; `Scope`'s constructor does `ASSERT_FALSE(g.inside); g.inside = true;`; its destructor resets `g.inside = false;`.

    Add `TEST_F(DialerTest, PeerDialRejectionNeverReentersSynchronously)`: reuse `DialRejectedByPeerDialGater`'s exact setup (`EXPECT_CALL(*gater, interceptPeerDial(pinfo.id)).WillOnce(Return(ConnectionGaterError::GATER_REJECTED_PEER_DIAL));`, `cmgr`/`tmgr` Times(0) expectations). Declare `ReentrancyGuard guard;` and `bool captured_reentrant = false;`. Wrap the `dialer->dial(pinfo, cb)` call in a block-scoped `{ ReentrancyGuard::Scope scope(guard); dialer->dial(pinfo, [&](auto &&rconn) { captured_reentrant = guard.inside; EXPECT_OUTCOME_FALSE(e, rconn); EXPECT_EQ(e.value(), (int)ConnectionGaterError::GATER_REJECTED_PEER_DIAL); }); }` -- the `Scope` destructs and clears `guard.inside` the instant `dial()` RETURNS, which happens BEFORE the deferred callback can ever fire (dial() only enqueues via scheduler_->schedule and returns immediately). After the scope closes, drain via the existing `scheduler_backend`-shift loop idiom already used in this file; assert `ASSERT_FALSE(captured_reentrant)`.

    Add `TEST_F(DialerTest, AddrDialRejectionNeverReentersSynchronously)`: mirror `DialRejectedByAddrDialGater`'s exact setup (`cmgr->getBestConnectionForPeer` returns nullptr, `gater->interceptAddrDial(pinfo.id, ma1)` returns `GATER_REJECTED_ADDR_DIAL`, `tmgr->findBest(ma1)` returns `transport`, `transport->dial` Times(0)). Same guard-wrap-and-capture technique around `dialer->dial(pinfo, cb)`; assert `captured_reentrant == false`.

    In `tcp_listener_test.cpp`, add `TEST_F(TcpListenerTest, AcceptGaterRejectionDeferredUntilSchedulerDrains)`: reuse `AcceptRejectedByGaterClosesConnectionWithoutUpgrading`'s exact real-socket setup (listen, `client_sock.async_connect`, `context->run_for(100ms)`). Add a `bool intercept_called = false;` set inside the `gater` mock's rejection action. Directly after the FIRST `context->run_for(100ms)` call (i.e. before draining `scheduler_backend`), assert `ASSERT_FALSE(intercept_called)` -- this proves `interceptAccept` has not been invoked purely from `async_accept`'s own completion (the io_context run loop), since the entire accept-handling block including the `interceptAccept` call lives inside `scheduler_->schedule(...)`, which the `ManualSchedulerBackend` (a queue entirely separate from `context`'s own run loop) has not yet drained. Then drain `scheduler_backend` and assert `ASSERT_TRUE(intercept_called)`. Add a short comment above this test explaining WHY it uses a before/after observation instead of the `ReentrancyGuard::Scope` idiom used elsewhere: `interceptAccept` is a synchronous, direct-return method with no callback-taking collaborator that can be forced into synchronous completion (D-06's technique does not apply to this specific call site -- see 03-RESEARCH.md's "Key nuance" paragraph), and the ENTIRE accept-handling block, interceptAccept call included, is already the deferred body of one `scheduler_->schedule(...)` call -- there is no further nested scheduling to guard around at this exact site.
  </action>
  <acceptance_criteria>
    dialer_test gains exactly 2 new passing TEST_F cases (PeerDialRejectionNeverReentersSynchronously, AddrDialRejectionNeverReentersSynchronously); both assert captured_reentrant == false.
    tcp_listener_test gains exactly 1 new passing TEST_F case (AcceptGaterRejectionDeferredUntilSchedulerDrains); it asserts intercept_called is false immediately after the io_context-only run_for(100ms) and true only after scheduler_backend fully drains.
    All pre-existing TEST_F cases in both files continue to pass (no regressions).
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build --config Debug --target dialer_test tcp_listener_test -- -m &amp;&amp; ctest --test-dir build -C Debug -R "dialer_test|tcp_listener_test" --output-on-failure</automated>
  </verify>
  <done>Both files build; all TEST_F cases (existing + new) pass under ctest.</done>
</task>

<task type="auto">
  <name>Task 2: upgrader_session_test.cpp -- interceptSecured/interceptUpgraded reentrancy</name>
  <files>test/libp2p/transport/upgrader_session_test.cpp</files>
  <read_first>
    test/libp2p/transport/upgrader_session_test.cpp (existing fixture; SecuredRejectedClosesAndDefersHandler and UpgradedRejectedClosesAndDefersHandler are the base cases being extended; SecuredAcceptedProceedsToMux/UpgradedAcceptedInvokesHandler are the success-path tests that must NOT get this guard)
    src/transport/impl/upgrader_session.cpp lines 68-120 (onSecured -- both rejection branches at 78-95 and 101-116 defer via scheduler_->schedule; the success chain at 98-118 does NOT)
    .planning/phases/03-hardening-live-validation-documentation/03-PATTERNS.md ("Full test case template" -- InterceptSecuredRejectionNeverReentersSynchronously worked example, verbatim reusable)
  </read_first>
  <action>
    Add the SAME `ReentrancyGuard`/`Scope` struct used in Task 1 at file scope, above the fixture (verbatim per 03-PATTERNS.md's scaffolding -- do not invent a variant).

    Add `TEST_F(UpgraderSessionTest, InterceptSecuredRejectionNeverReentersSynchronously)`: copy `SecuredRejectedClosesAndDefersHandler`'s exact EXPECT_CALL setup (`upgradeToSecureInbound` synchronous success via the `UpgradeToSecureInbound` action, `secure->remotePeer/remoteMultiaddr/isInitiatorMock`, `gater->interceptSecured(...)` returning `ConnectionGaterError::GATER_REJECTED_SECURED`, `secure->isClosed/close`, `upgrader->upgradeToMuxed` `Times(0)`). Declare `ReentrancyGuard guard;` and `bool handler_fired_reentrant = false;`. In `EXPECT_CALL(handler_cb, Call(_)).WillOnce(Invoke([&](auto &&r) { handler_fired_reentrant = guard.inside; EXPECT_OUTCOME_FALSE(e, r); EXPECT_EQ(e.value(), (int)ConnectionGaterError::GATER_REJECTED_SECURED); }));`. Wrap `session->secureInbound();` in `{ ReentrancyGuard::Scope scope(guard); session->secureInbound(); }`. Immediately after the scope closes (BEFORE calling `pump()`), assert `ASSERT_FALSE(handler_fired_reentrant)`. Then call `pump()` and reuse the existing `executed` bool pattern to confirm the handler eventually fired. This directly targets `upgrader_session.cpp`'s `scheduler_->schedule([self, err]{ self->handler_(err); })` at the interceptSecured rejection branch (lines 90-94).

    Add `TEST_F(UpgraderSessionTest, InterceptUpgradedRejectionNeverReentersSynchronously)`: same technique, based on `UpgradedRejectedClosesAndDefersHandler`'s setup (interceptSecured succeeds, `upgradeToMuxed` succeeds via the `UpgradeToMuxed` action, `interceptUpgraded` returns `ConnectionGaterError::GATER_REJECTED_UPGRADED`, `capable->remotePeer/isClosed/close`), targeting the `self->scheduler_->schedule([self, err]{ self->handler_(err); })` call inside the `upgradeToMuxed` completion lambda's rejection branch (lines 113-115). Wrap `session->secureInbound()` in the guard `Scope` the same way; assert `handler_fired_reentrant == false` before `pump()`.

    Add a brief comment directly above these two new tests noting -- matching RESEARCH.md's "Anti-Patterns to Avoid" -- that the pre-existing `SecuredAcceptedProceedsToMux`/`UpgradedAcceptedInvokesHandler` SUCCESS-path tests are intentionally NOT extended with this guard, because `onSecured`'s success chain calls `handler_` directly with no `scheduler_->schedule` hop (verified at `upgrader_session.cpp:98-118`) -- asserting non-reentrancy there would assert something false about the current, unmodified production code.
  </action>
  <acceptance_criteria>
    upgrader_session_test gains exactly 2 new passing TEST_F cases; all 4 pre-existing cases continue to pass.
    A source grep for a guard-flag assertion (e.g. "guard.inside") inside `SecuredAcceptedProceedsToMux` or `UpgradedAcceptedInvokesHandler` returns no matches -- confirms the success-path exclusion was honored.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build --config Debug --target upgrader_session_test -- -m &amp;&amp; ctest --test-dir build -C Debug -R upgrader_session_test --output-on-failure</automated>
  </verify>
  <done>Both new tests assert handler_fired_reentrant == false immediately inside the scope, before pump(); the handler is confirmed to eventually fire only after pump() drains the scheduler.</done>
</task>

<task type="auto">
  <name>Task 3: pnet_protected_connection_test.cpp -- read/write completion reentrancy</name>
  <files>test/libp2p/security/pnet/pnet_protected_connection_test.cpp</files>
  <read_first>
    test/libp2p/security/pnet/pnet_protected_connection_test.cpp (existing PipeEnd/Fixture helpers and CompletionDeferredThroughScheduler, the base case being upgraded to an explicit guard)
    src/security/pnet/pnet_protected_connection.cpp lines 196-205 (deferReadCallback/deferWriteCallback -- the two unconditional scheduler_->schedule(...) call sites, applying to BOTH success and failure completions)
  </read_first>
  <action>
    Add the same `ReentrancyGuard`/`Scope` struct at file scope, inside the existing anonymous namespace alongside `PipeEnd`/`Fixture`/`makePsk`/`makePattern` (matching this file's existing style of keeping helpers in the anonymous namespace).

    Add `TEST(PnetProtectedConnectionTest, WriteCompletionNeverReentersSynchronously)`: reuse the exact two-`PipeEnd` setup from `CompletionDeferredThroughScheduler` (`end_a`/`end_b` peered, `prot_a` built over `end_a` with `kPskABytes`). Declare `ReentrancyGuard guard;` and `bool fired_reentrant = false;`. Wrap `prot_a->write(payload, payload.size(), cb)` in `{ ReentrancyGuard::Scope scope(guard); prot_a->write(payload, payload.size(), [&](auto) { fired_reentrant = guard.inside; }); }`. Assert `ASSERT_FALSE(fired_reentrant)` immediately after the scope closes -- BEFORE calling `fx.drain()`. Then `fx.drain()` and reuse the `fired`-style pattern to confirm the callback eventually ran. `PipeEnd::writeSome` already completes its inner callback synchronously inline (`cb(n);`) -- this is already the D-06 synchronous-completion technique, no new mock needed -- so this directly proves `deferWriteCallback`'s `scheduler_->schedule(...)` wrap is load-bearing.

    Add `TEST(PnetProtectedConnectionTest, ReadCompletionNeverReentersSynchronously)`: same technique on the read path -- prime `prot_b->read(got, N, cb)` wrapped in the guard `Scope` (matching `RoundTripSamePskBothDirections`'s A-to-B setup for `end_a`/`end_b`/`prot_a`/`prot_b`), then `prot_a->write(payload, payload.size(), [](auto) {})` to trigger delivery. Assert `fired_reentrant == false` immediately after the read-wrapping scope closes and BEFORE `fx.drain()`.
  </action>
  <acceptance_criteria>
    pnet_protected_connection_test gains exactly 2 new passing tests; all 7 pre-existing tests continue passing.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build --config Debug --target pnet_protected_connection_test -- -m &amp;&amp; ctest --test-dir build -C Debug -R pnet_protected_connection_test --output-on-failure</automated>
  </verify>
  <done>Both new tests assert the guard is false immediately after the triggering call returns, proving deferReadCallback/deferWriteCallback never invoke the caller's callback inline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| external collaborator completion (mocked Upgrader/RawConnection/gater result) -> internal dial/upgrade/pnet state | A synchronously-completing collaborator is the adversarial condition under test; production code must not let that synchronicity leak into a reentrant call against its own in-flight state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Elevation of Privilege | scheduler_->schedule call sites across DialerImpl/UpgraderSession/PnetProtectedConnection, regression-guarded by this plan | high | mitigate | 7 new/extended TEST_F/TEST cases directly assert no rejection/completion callback fires synchronously inline, guarding the exact mitigation (deferred dispatch) that prevents a reentrant call from corrupting in-flight dial/upgrade/pnet state |
| T-03-05 | Tampering | interceptAccept accept-then-reject window in TcpListener | medium | mitigate | New AcceptGaterRejectionDeferredUntilSchedulerDrains test proves the gater check and socket close only run from the scheduler-deferred block, never from the raw acceptor completion frame |
</threat_model>

<verification>
All 4 modified files build; ctest reports all pre-existing tests still passing plus the 7 new reentrancy-regression cases passing. No production source files are touched.
</verification>

<success_criteria>
Every scheduler_->schedule call site identified in 03-RESEARCH.md's verified defer/no-defer table (excluding the UpgraderSession success chain, which correctly has no guard) has a dedicated regression test proving deferred, non-reentrant delivery.
</success_criteria>

<output>
Create `.planning/phases/03-hardening-live-validation-documentation/03-02-SUMMARY.md` when done
</output>
