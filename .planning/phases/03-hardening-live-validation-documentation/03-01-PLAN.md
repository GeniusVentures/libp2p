---
phase: 03-hardening-live-validation-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/acceptance/p2p/pnet/pnet_two_node_test.cpp
  - test/acceptance/p2p/pnet/CMakeLists.txt
  - test/acceptance/p2p/CMakeLists.txt
autonomous: true
requirements: [TEST-03]
user_setup: []

must_haves:
  truths:
    - "A node configured with the correct 256-bit PSK can dial and open an echo stream to a peer configured with the same PSK (per D-01/D-02)"
    - "A node with a missing/mismatched PSK never establishes a usable stream to a PSK-protected peer, observed black-box within a bounded timeout, with no assertion on a specific PnetError value (per D-03)"
  artifacts:
    - test/acceptance/p2p/pnet/pnet_two_node_test.cpp
    - test/acceptance/p2p/pnet/CMakeLists.txt
  key_links:
    - "pnet_two_node_test links p2p_pnet_upgrader/p2p_pnet so makeHostInjector(usePrivateNetwork(...)) resolves PnetUpgraderDecorator through the real DI graph, not a mock"
    - "test/acceptance/p2p/CMakeLists.txt add_subdirectory(pnet) registers the target in the default ctest suite (D-04, no new CMake gating option)"
---

<objective>
Build a purpose-built, two-node, real-TCP-loopback acceptance test proving the pnet PSK boundary works end-to-end: matched-PSK peers connect and exchange an echo stream; mismatched/missing-PSK peers never establish a usable connection. This is the only test in the phase exercising the FULL DI-assembled `Host` (via `makeHostInjector(usePrivateNetwork(...))`) rather than mocked collaborators — Phase 2's own SUMMARY explicitly deferred this exact validation to Phase 3.

Purpose: TEST-03 / ROADMAP Phase 3 success criterion #1 — prove PNET-01/02/03 hold under live conditions, not just Phase 2's mocked unit tests.
Output: `test/acceptance/p2p/pnet/pnet_two_node_test.cpp` (2 TEST_F cases) + its CMake registration, running in the default `ctest` suite.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hardening-live-validation-documentation/03-CONTEXT.md
@.planning/phases/03-hardening-live-validation-documentation/03-RESEARCH.md
@.planning/phases/03-hardening-live-validation-documentation/03-PATTERNS.md

@test/acceptance/p2p/host/host_integration_test.cpp
@test/acceptance/p2p/host/peer/test_peer.cpp
@test/acceptance/p2p/host/peer/test_peer.hpp
@test/acceptance/p2p/host/CMakeLists.txt
@test/acceptance/p2p/CMakeLists.txt
@test/libp2p/security/pnet/pnet_injector_test.cpp
@include/libp2p/injector/network_injector.hpp
@include/libp2p/injector/host_injector.hpp
@example/01-echo/libp2p_echo_client.cpp
@.planning/phases/02-private-network-pnet-psk-protector/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the two-node PSK fixture and the matched-PSK positive case</name>
  <files>test/acceptance/p2p/pnet/pnet_two_node_test.cpp, test/acceptance/p2p/pnet/CMakeLists.txt, test/acceptance/p2p/CMakeLists.txt</files>
  <read_first>
    test/acceptance/p2p/host/host_integration_test.cpp (PeerPromise/PeerFuture readiness-sync pattern, lines 45-55 and 76-91; TearDown at 48-51)
    test/acceptance/p2p/host/peer/test_peer.cpp (per-node io_context + dedicated thread lifecycle: Peer::startServer's context_->post(...)+thread_ construction, Peer::wait()/~Peer())
    test/acceptance/p2p/host/CMakeLists.txt and test/acceptance/p2p/CMakeLists.txt (addtest()/add_subdirectory registration pattern)
    test/libp2p/security/pnet/pnet_injector_test.cpp (kValidSwarmKey literal and usePrivateNetwork call shape, lines 36-39, 48-50)
    include/libp2p/injector/network_injector.hpp (usePrivateNetwork doc comment and body, lines 258-327 — eager PskValidationError throw semantics)
    example/01-echo/libp2p_echo_client.cpp (host->newStream(...) dial pattern, lines 90-156)
  </read_first>
  <action>
    Create `test/acceptance/p2p/pnet/pnet_two_node_test.cpp` with a `PnetTwoNodeTest : public ::testing::Test` fixture (no `TestWithParam`, per D-02's explicit rejection of `HostIntegrationTest`'s parametrized fixture).

    Define a private nested `Node` struct: `std::shared_ptr&lt;boost::asio::io_context&gt; io_context`, `std::shared_ptr&lt;libp2p::Host&gt; host`, `std::shared_ptr&lt;libp2p::protocol::Echo&gt; echo`, `std::thread thread`. Its destructor joins `thread` (if joinable) then calls `host-&gt;stop()`, mirroring `test_peer.hpp`'s `Peer::wait()`/`~Peer()` pattern exactly.

    Add a fixture method `makeNode(std::string_view swarm_key_text)` returning `std::shared_ptr&lt;Node&gt;`: build `auto injector = libp2p::injector::makeHostInjector(libp2p::injector::usePrivateNetwork(swarm_key_text));`, resolve `injector.create&lt;std::shared_ptr&lt;libp2p::Host&gt;&gt;()` and `injector.create&lt;std::shared_ptr&lt;boost::asio::io_context&gt;&gt;()` into the node, construct `std::make_shared&lt;libp2p::protocol::Echo&gt;()` (default-config, matching `test_peer.cpp`'s `echo_{std::make_shared&lt;Echo&gt;()}`, not the CLI-configured echo example), and call `host-&gt;setProtocolHandler({echo-&gt;getProtocolId()}, handler)` where `handler` invokes `echo-&gt;handle(std::move(stream))`.

    Add a fixture method `startListening(std::shared_ptr&lt;Node&gt; node, const multi::Multiaddress &amp;ma, std::shared_ptr&lt;std::promise&lt;peer::PeerInfo&gt;&gt; promise, std::chrono::milliseconds run_for)` matching `test_peer.cpp::Peer::startServer`'s two-phase structure exactly: `node-&gt;io_context-&gt;post([node, ma, promise] { ASSERT_TRUE(node-&gt;host-&gt;listen(ma)) ...; node-&gt;host-&gt;start(); promise-&gt;set_value(node-&gt;host-&gt;getPeerInfo()); });` then `node-&gt;thread = std::thread([node, run_for] { node-&gt;io_context-&gt;run_for(run_for); });`.

    Define two file-scope `constexpr std::string_view` literals: `kSwarmKeyMatched` = `/key/swarm/psk/1.0.0/\n/base16/000102030405060708090a0b0c0d0e0f101112131415161718191a1b1c1d1e1f\n` (byte-identical to `pnet_injector_test.cpp`'s `kValidSwarmKey`) and, for Task 2's later use, `kSwarmKeyMismatched` = `/key/swarm/psk/1.0.0/\n/base16/fffefdfcfbfaf9f8f7f6f5f4f3f2f1f0efeeedecebeae9e8e7e6e5e4e3e2e1e0\n` (byte-identical key pattern to `dialer_test.cpp`'s and `pnet_protected_connection_test.cpp`'s `kPskBBytes`, reformatted as hex).

    Write `TEST_F(PnetTwoNodeTest, MatchedPskConnectsAndOpensEchoStream)`: call `testutil::prepareLoggers()`; build `server = makeNode(kSwarmKeyMatched)` and `client = makeNode(kSwarmKeyMatched)`; construct a `std::promise&lt;peer::PeerInfo&gt;`/`std::shared_future&lt;peer::PeerInfo&gt;` pair via `std::make_shared` (matching `host_integration_test.cpp`'s `PeerPromise`/`PeerFuture` aliases); call `startListening(server, "/ip4/127.0.0.1/tcp/40520"_multiaddr, promise, 3s)`; assert `future.wait_for(2s) == std::future_status::ready` (bounded-hang guard, matching `host_integration_test.cpp` line 90's idiom), then `auto server_info = future.get();`. Post a dial-and-open-stream lambda onto `client-&gt;io_context` calling `client-&gt;host-&gt;newStream(server_info, {client-&gt;echo-&gt;getProtocolId()}, cb)` where `cb` fulfills a HEAP-ALLOCATED `std::shared_ptr&lt;std::promise&lt;bool&gt;&gt;` with `r.has_value()` — heap-allocated specifically so the promise safely outlives the calling stack frame if the bounded wait below ever times out while the background client thread is still running (a stack-local promise would risk a use-after-scope write). Start `client-&gt;thread` running `client-&gt;io_context-&gt;run_for(3s)`. Assert the stream-opened future becomes ready within 2s and its value is `true`.

    Create `test/acceptance/p2p/pnet/CMakeLists.txt`: `addtest(pnet_two_node_test pnet_two_node_test.cpp)` plus `target_link_libraries(pnet_two_node_test p2p_default_network p2p_basic_host p2p_pnet p2p_pnet_upgrader p2p_peer_repository p2p_inmem_address_repository p2p_inmem_key_repository p2p_inmem_protocol_repository p2p_protocol_echo p2p_multiaddress p2p_testutil p2p_literals)` — this exact block is the verified pattern from `03-PATTERNS.md`'s CMake registration section.

    Edit `test/acceptance/p2p/CMakeLists.txt` (use Edit, not Write — existing file) to add one line `add_subdirectory(pnet)` immediately after the existing `add_subdirectory(host)` line (D-04 — no new CMake gating option needed; `addtest()` already wires the target into the default `ctest` run).

    Informational note, record only, do not act beyond noting: Boost.DI resolves `DialerImpl`'s `psk` constructor parameter (`std::shared_ptr&lt;const security::pnet::Psk&gt;`) via its default-argument arity fallback — confirmed empirically this session: `network_injector_test.cpp`'s `NetworkBuilder.DefaultBuilds` and `host_injector_test.cpp`'s `HostInjector.Default` already build a full `Dialer` through `makeNetworkInjector()`/`makeHostInjector()` with zero explicit Psk binding and pass on this exact build. This means `Dialer`'s `psk_` stays `nullptr` even under `usePrivateNetwork(...)`, since that module only binds `PskHandle` and rebinds `Upgrader` — it never binds `shared_ptr&lt;const Psk&gt;`. The pnet wire-level boundary this test validates (`PnetUpgraderDecorator`/`PnetProtectedConnection`) is unaffected — it gets the PSK via the separate `PskHandle` binding — but Phase 2's BOOT-01 bootstrap-dial-refusal is consequently NOT exercised through this DI path (it is unit-tested today only via `DialerImpl`'s manual 7-arg constructor in `dialer_test.cpp`'s `makePskDialer()`). This is a pre-existing Phase 2 DI-wiring gap, outside this phase's "no production code changes" boundary — do not fix it; if independently reconfirmed while executing this task, add one sentence noting it under this plan's SUMMARY.md, nothing more.
  </action>
  <acceptance_criteria>
    `cmake --build build --config Debug --target pnet_two_node_test -- -m` succeeds.
    `ctest --test-dir build -C Debug -R pnet_two_node_test --output-on-failure` reports `PnetTwoNodeTest.MatchedPskConnectsAndOpensEchoStream` passing.
    Server and client hosts both resolve `Upgrader` to `PnetUpgraderDecorator` implicitly via `usePrivateNetwork(...)` (no explicit assertion needed — this is structurally guaranteed by the injector module already unit-tested in `pnet_injector_test.cpp`).
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build --config Debug --target pnet_two_node_test -- -m &amp;&amp; ctest --test-dir build -C Debug -R pnet_two_node_test --output-on-failure</automated>
  </verify>
  <done>The matched-PSK positive case builds and passes under ctest; the echo stream opens successfully within the bounded wait; test/acceptance/p2p/CMakeLists.txt and the new pnet/CMakeLists.txt correctly register the target in the default ctest suite.</done>
</task>

<task type="auto">
  <name>Task 2: Add the mismatched-PSK negative case (fresh node pair, black-box rejection per D-03)</name>
  <files>test/acceptance/p2p/pnet/pnet_two_node_test.cpp</files>
  <read_first>
    test/acceptance/p2p/pnet/pnet_two_node_test.cpp (the fixture Task 1 created — Node/makeNode/startListening, kSwarmKeyMatched/kSwarmKeyMismatched)
    .planning/phases/03-hardening-live-validation-documentation/03-CONTEXT.md (D-02 "exactly 2 nodes, purpose-built fixture" and D-03 "black-box, no specific PnetError")
    .planning/phases/03-hardening-live-validation-documentation/03-RESEARCH.md ("Pattern 1 ... D-03 mismatched-PSK assertion shape" and "Open Question #2" sections)
  </read_first>
  <action>
    Add `TEST_F(PnetTwoNodeTest, MismatchedPskNeverEstablishesUsableStream)` to the same file, constructing a FRESH node pair — per RESEARCH.md's Open Question #2 recommendation, do NOT reuse Task 1's `server`/`client` objects or their PSK state: `server2 = makeNode(kSwarmKeyMatched)` (the reference private network keeps the "matched" key) and `attacker = makeNode(kSwarmKeyMismatched)` (the dialing peer holds a different 256-bit key). Listen `server2` on `"/ip4/127.0.0.1/tcp/40521"_multiaddr` — a DIFFERENT port from Task 1's 40520, avoiding TIME_WAIT collisions across the two TEST_F cases in one binary run — using the same `startListening`/future-wait pattern as Task 1.

    From `attacker`, post `attacker-&gt;host-&gt;newStream(server2_info, {attacker-&gt;echo-&gt;getProtocolId()}, cb)` where `cb` fulfills a heap-allocated `std::shared_ptr&lt;std::promise&lt;bool&gt;&gt;` with `r.has_value()`, run `attacker-&gt;thread` for `3s`.

    Assert per D-03's literal black-box framing: compute `auto status = stream_future.wait_for(2s);` then branch — if `status == std::future_status::ready`, `ASSERT_FALSE(stream_future.get())` (the callback fired and it MUST be a failure, never success); if `status == std::future_status::timeout`, that is ALSO a passing outcome per D-03 — "the attempt fails at the pnet layer and never reaches multiselect" covers a permanent hang just as validly as an explicit error, so the timeout branch must NOT fail the test. Do NOT assert on any specific `libp2p::security::pnet::PnetError` value anywhere in this test — D-03 explicitly prohibits that stronger claim.

    Do not add any new CMake registration — Task 1 already wired the `pnet_two_node_test` target; this task only adds a second `TEST_F` case to the existing file.
  </action>
  <acceptance_criteria>
    `ctest --test-dir build -C Debug -R pnet_two_node_test --output-on-failure` reports both `PnetTwoNodeTest.MatchedPskConnectsAndOpensEchoStream` and `PnetTwoNodeTest.MismatchedPskNeverEstablishesUsableStream` passing.
    A source grep for `PnetError` inside the new `MismatchedPskNeverEstablishesUsableStream` test body returns no matches (D-03 compliance — no specific error code asserted).
    Server listens on port 40521, distinct from Task 1's 40520.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build --config Debug --target pnet_two_node_test -- -m &amp;&amp; ctest --test-dir build -C Debug -R pnet_two_node_test --output-on-failure</automated>
  </verify>
  <done>pnet_two_node_test target has exactly 2 TEST_F cases, both passing; the mismatched case's assertion structurally accepts EITHER an explicit failure OR a bounded timeout as the passing condition, matching D-03 exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| attacker-node -> TCP loopback -> PSK-protected server | Untrusted raw TCP bytes cross this boundary before any protocol negotiation; the pnet PSK check is the first thing that touches them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Spoofing | pnet PSK boundary (PnetUpgraderDecorator/PnetProtectedConnection), validated live by this plan | high | mitigate | Live two-node test proves a peer with a mismatched 256-bit PSK never completes multiselect/protocol negotiation over a real TCP socket — the XSalsa20 keystream mismatch produces undecryptable garbage past the cleartext nonce exchange; this is Phase 2's already-implemented control, this plan is the regression proof, no code change |
| T-03-02 | Information Disclosure | TCP wire bytes ahead of the PSK check | medium | accept | Already covered by Phase 2's PnetProtectedConnection wrapping every raw connection before multiselect (unit-tested in 02-03); this phase adds no new exposure, the live test only re-confirms under real sockets |
| T-03-03 | Tampering | DialerImpl's psk_ ctor param not wired through usePrivateNetwork's DI graph (documented in Task 1) | low | accept | BOOT-01 bootstrap-refusal not exercised via the DI-injector construction path is a pre-existing Phase 2 gap outside this phase's charter to fix; TEST-03's own assertions (PNET-01/02/03) do not depend on Dialer's psk_ field at all |
</threat_model>

<verification>
Both TEST_F cases in pnet_two_node_test pass under ctest, registered in the default suite via test/acceptance/p2p/CMakeLists.txt's add_subdirectory(pnet). No production code files are modified by this plan.
</verification>

<success_criteria>
Live two-node PSK acceptance test exists, builds, and passes: matched PSK connects and exchanges a stream; mismatched PSK never establishes one (fails or times out, no specific error asserted).
</success_criteria>

<output>
Create `.planning/phases/03-hardening-live-validation-documentation/03-01-SUMMARY.md` when done
</output>
