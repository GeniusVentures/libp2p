---
phase: 02-private-network-pnet-psk-protector
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - include/libp2p/transport/impl/pnet_upgrader_decorator.hpp
  - src/transport/impl/pnet_upgrader_decorator.cpp
  - src/transport/impl/CMakeLists.txt
  - include/libp2p/injector/network_injector.hpp
  - include/libp2p/network/impl/dialer_impl.hpp
  - src/network/impl/dialer_impl.cpp
  - test/libp2p/security/pnet/pnet_upgrader_decorator_test.cpp
  - test/libp2p/security/pnet/CMakeLists.txt
  - test/libp2p/security/pnet/pnet_injector_test.cpp
  - test/libp2p/network/dialer_test.cpp
autonomous: true
requirements: [PNET-01, PNET-04, PNET-05, BOOT-01, TEST-02]
must_haves:
  truths:
    - "Applying usePrivateNetwork(validKey) makes every upgraded raw connection pass through PnetUpgraderDecorator → PnetProtectedConnection on BOTH dial and accept paths (PNET-01/PNET-04, D-07)"
    - "Calling usePrivateNetwork with ANY invalid key throws PskValidationError before any Host object can exist (PNET-05, D-09/D-10)"
    - "Building an injector WITHOUT usePrivateNetwork produces a plain UpgraderImpl and a null Psk in DialerImpl — behavior identical to today (D-08)"
    - "With a Psk present, a dial to /dnsaddr/bootstrap.libp2p.io or a public-bootstrap peer ID is refused inside DialerImpl with PNET_PUBLIC_BOOTSTRAP_REFUSED, logged SL_DEBUG, error delivered via scheduler — silent to unrelated callers (D-11/D-12/D-13)"
    - "Without a Psk, dials (including to the bootstrap address) proceed exactly as before"
    - "Relay upgrade overloads and upgradeToMuxed pass through the decorator unwrapped (documented limitation, research A4)"
  artifacts:
    - path: "include/libp2p/transport/impl/pnet_upgrader_decorator.hpp"
      provides: "Upgrader decorator wrapping the two RawSPtr methods"
      contains: "PnetUpgraderDecorator"
    - path: "include/libp2p/injector/network_injector.hpp"
      provides: "usePrivateNetwork(key) combined DI module + PskValidationError"
      contains: "usePrivateNetwork"
    - path: "test/libp2p/security/pnet/pnet_upgrader_decorator_test.cpp"
      provides: "Method-coverage matrix test"
      contains: "TEST"
    - path: "test/libp2p/security/pnet/pnet_injector_test.cpp"
      provides: "DI smoke test: valid module / invalid throws / absence = public mode"
      contains: "TEST"
  key_links:
    - from: "include/libp2p/injector/network_injector.hpp"
      to: "transport::PnetUpgraderDecorator"
      via: "di::bind<transport::Upgrader>().to<PnetUpgraderDecorator>()[boost::di::override]"
      pattern: "to<PnetUpgraderDecorator>"
    - from: "src/network/impl/dialer_impl.cpp"
      to: "PnetError::PNET_PUBLIC_BOOTSTRAP_REFUSED"
      via: "dial-time refusal callback scheduled via scheduler_"
      pattern: "PNET_PUBLIC_BOOTSTRAP_REFUSED"
    - from: "src/transport/impl/pnet_upgrader_decorator.cpp"
      to: "security::pnet::PnetProtectedConnection"
      via: "wrap RawSPtr before forwarding to inner UpgraderImpl"
      pattern: "PnetProtectedConnection"
---

<objective>
Wire the pnet layer into the application graph and the dial path: `PnetUpgraderDecorator` (the choke point both dial and accept upgrade paths already route through), the `usePrivateNetwork(key)` combined DI module (one line turns on private-network mode, fails fast on invalid keys), and the dial-time public-bootstrap refusal in `DialerImpl`.

Purpose: PNET-04 (DI-configured, not hardcoded), PNET-05 (fail-safe private-network mode), BOOT-01 (no public DHT leakage), and completion of PNET-01's "applied before negotiation begins on both paths".

Output: `p2p_pnet_upgrader` leaf target, `usePrivateNetwork` module in `network_injector.hpp`, `psk`-aware `DialerImpl`, and three test suites (decorator matrix, DI smoke, dialer refusal).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-private-network-pnet-psk-protector/02-CONTEXT.md
@.planning/phases/02-private-network-pnet-psk-protector/02-RESEARCH.md
@.planning/phases/02-private-network-pnet-psk-protector/02-PATTERNS.md

@include/libp2p/transport/upgrader.hpp
@include/libp2p/transport/impl/upgrader_impl.hpp
@include/libp2p/injector/network_injector.hpp
@include/libp2p/network/impl/dialer_impl.hpp
@src/network/impl/dialer_impl.cpp
@include/libp2p/peer/address_repository.hpp
@test/libp2p/network/dialer_test.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: PnetUpgraderDecorator + method-coverage test</name>
  <files>include/libp2p/transport/impl/pnet_upgrader_decorator.hpp, src/transport/impl/pnet_upgrader_decorator.cpp, src/transport/impl/CMakeLists.txt, test/libp2p/security/pnet/pnet_upgrader_decorator_test.cpp, test/libp2p/security/pnet/CMakeLists.txt</files>
  <read_first>
    - include/libp2p/transport/upgrader.hpp (all five methods incl. the two StrSPtr relay overloads — the coverage matrix)
    - include/libp2p/transport/impl/upgrader_impl.hpp + src/transport/impl/upgrader_impl.cpp (class-shape analog: Upgrader + enable_shared_from_this, ctor deps)
    - src/transport/impl/CMakeLists.txt (target layout; prefer a separate p2p_pnet_upgrader leaf so p2p_upgrader stays pnet-free — Pitfall 6)
    - include/libp2p/security/pnet/pnet_protected_connection.hpp (the wrapper produced, from Plan 03)
    - include/libp2p/security/pnet/psk.hpp + include/libp2p/crypto/xsalsa20/xsalsa20.hpp (dependencies from Plans 01/02)
    - test/libp2p/network/dialer_test.cpp (mock-harness + ManualSchedulerBackend drain pattern to reuse in the decorator test)
    - .planning/phases/02-private-network-pnet-psk-protector/02-PATTERNS.md §pnet_upgrader_decorator assignment (ctor signature + method coverage table — the concrete form)
    - .planning/phases/02-private-network-pnet-psk-protector/02-RESEARCH.md Pattern 2 (concrete inner type avoids Boost.DI recursion, A1) and Open Question 1 resolution (relay pass-through documented, A4)
  </read_first>
  <action>
Create class libp2p::transport::PnetUpgraderDecorator : public Upgrader, public std::enable_shared_from_this<PnetUpgraderDecorator> (guard LIBP2P_TRANSPORT_IMPL_PNET_UPGRADER_DECORATOR_HPP), in include/libp2p/transport/impl/ + src/transport/impl/ — ctor takes the CONCRETE type (RESEARCH Pattern 2 — requesting the Upgrader interface it replaces would create Boost.DI circular self-resolution, A1): PnetUpgraderDecorator(std::shared_ptr<UpgraderImpl> inner, std::shared_ptr<const security::pnet::Psk> psk, std::shared_ptr<basic::Scheduler> scheduler).

Method coverage (exact matrix from PATTERNS):
- upgradeToSecureOutbound(RawSPtr, remoteId, cb) and upgradeToSecureInbound(RawSPtr, cb): wrap conn in std::make_shared<security::pnet::PnetProtectedConnection>(conn, psk_, scheduler_) and forward the WRAPPED conn to inner_ with the original callback.
- upgradeToSecureOutboundRelay(StrSPtr, ...) and upgradeToSecureInboundRelay(StrSPtr, ...): pass through UNCHANGED to inner_ — add a header doc comment noting the deliberate limitation: relay streams are not PSK-wrapped this phase; private-network circuit relay would require a full Stream decorator (research A4 / Open Q1 resolution — explicit, not silent scope-cut).
- upgradeToMuxed(SecSPtr, cb): pass through (muxed operates above the PSK layer).

CMake: NEW leaf target p2p_pnet_upgrader in src/transport/impl/CMakeLists.txt (libp2p_add_library(p2p_pnet_upgrader pnet_upgrader_decorator.cpp)) linking p2p_upgrader + p2p_pnet — keeps p2p_upgrader itself pnet-free per Pitfall 6. Do NOT add the decorator source to p2p_upgrader's source list.

Test (test/libp2p/security/pnet/pnet_upgrader_decorator_test.cpp, addtest in the pnet test CMakeLists linking p2p_pnet_upgrader, p2p_manual_scheduler_backend, gmock): mock the inner as an UpgraderImpl-substitute or GMock Upgrader — since the ctor demands concrete UpgraderImpl, either construct a real UpgraderImpl with mocked collaborators (mirror how upgrader tests build it) or template/guard the test seam — check test/libp2p/transport/ for existing upgrader test harness and reuse its fixture. Cases per RESEARCH Test Matrix: (1) upgradeToSecureOutbound/Inbound forward a conn whose dynamic type is PnetProtectedConnection (capture the RawSPtr argument in the mock and dynamic_pointer_cast-assert); (2) relay overloads + upgradeToMuxed forward the SAME pointer (identity compare) — no wrapping; (3) with the real-UpgraderImpl route, the wrapped conn reaches multiselect (optional if the mock route is clean).
  </action>
  <verify>cmake --build build --target pnet_upgrader_decorator_test --config Debug && ctest -C Debug -R pnet_upgrader_decorator_test --output-on-failure (run from build/)</verify>
  <acceptance_criteria>
    - pnet_upgrader_decorator.hpp declares the ctor taking std::shared_ptr<UpgraderImpl> (the concrete type, not the interface)
    - The decorator source references PnetProtectedConnection in exactly the two RawSPtr methods; relay + muxed methods contain no wrapper construction (identity pass-through)
    - src/transport/impl/CMakeLists.txt defines target p2p_pnet_upgrader and p2p_upgrader's source list does NOT include pnet_upgrader_decorator.cpp
    - Header doc comment near the relay overloads mentions relay/Stream decoration limitation
    - ctest -C Debug -R pnet_upgrader_decorator_test passes with the wrap-assert (dynamic cast) and the identity-passthrough asserts
  </acceptance_criteria>
  <done>Decorator builds as p2p_pnet_upgrader, wraps exactly the two RawSPtr upgrade paths, passes relay/muxed through with documented rationale, all proven by tests.</done>
</task>

<task type="auto">
  <name>Task 2: usePrivateNetwork DI module + fail-fast validation + DI smoke test</name>
  <files>include/libp2p/injector/network_injector.hpp, test/libp2p/security/pnet/pnet_injector_test.cpp, test/libp2p/security/pnet/CMakeLists.txt</files>
  <read_first>
    - include/libp2p/injector/network_injector.hpp (useConnectionGater shape ~lines 243-254, default-binding block ~272-338, variadic composition slot ~line 338 — self-analog, exact)
    - include/libp2p/injector/host_injector.hpp (verify args forward into makeNetworkInjector — verify, don't assume)
    - .planning/phases/02-private-network-pnet-psk-protector/02-PATTERNS.md §network_injector assignment (full module-body shape to adapt) and §DI module shared pattern rules
    - .planning/phases/02-private-network-pnet-psk-protector/02-RESEARCH.md Pitfalls 4/5 (DI recursion + validation-locus resolution), Pattern 2/3 (A1/A2 assumptions this task's compile check resolves), and §Test Matrix injector/DI row
    - include/libp2p/security/pnet/psk.hpp (factory surface to call)
    - .planning/codebase/CONCERNS.md (MSVC soralog break — keep the DI test target's link set off yamux-dependent libs; if a graph-true host build is blocked, verify wiring through the minimal injector composition)
  </read_first>
  <action>
In include/libp2p/injector/network_injector.hpp, next to useConnectionGater:

1. `struct PskValidationError : std::runtime_error` — carries the security::pnet::PnetError code (message built from the error's "pnet:" category text; NEVER embeds key bytes).

2. `template <typename PskArg> inline auto usePrivateNetwork(PskArg &&key)` (D-07 single combined module): first line calls the appropriate Psk factory for the argument shape (string_view variants: try fromSwarmKeyText semantics via a small dispatch — if the input contains the /key/swarm/ header treat as swarm text, else base16, else base64; a pre-validated Psk argument binds directly as the exception-free overload). On factory failure → throw PskValidationError immediately (eager, BEFORE di::make_injector assembles — D-09/D-10, RESEARCH Pitfall 5 resolution: no Host can ever exist half-configured). On success: return boost::di::make_injector( bind<std::shared_ptr<const security::pnet::Psk>>().to(std::make_shared<const Psk>(std::move(psk.value()))), bind<transport::Upgrader>().template to<transport::PnetUpgraderDecorator>()[boost::di::override] ). Also provide the exception-free overload usePrivateNetwork(security::pnet::Psk validated) for integrators holding a validated key.
   - Deployment note for the concrete-inner-type binding: PnetUpgraderDecorator takes shared_ptr<UpgraderImpl> (Task 1) so Boost.DI must auto-construct UpgraderImpl from its already-bound deps (A1). resolve this assumption NOW: if the Soramitsu fork does not auto-construct, add a companion bind<UpgraderImpl>().to<UpgraderImpl>() (stateless clarification binding) inside the module — whatever makes the graph resolve; the smoke test below is the gate.
   - D-08 rule: add NO default binding for shared_ptr<const Psk> anywhere in makeNetworkInjector. If DialerImpl's nullable param (Task 3) cannot resolve when the module is absent (A2), add the minimal di::bind<std::shared_ptr<const security::pnet::Psk>>().to(nullptr) DEFAULT ONLY in the base injector — after confirming via compile; absence must remain the public-mode signal.

3. DI smoke test test/libp2p/security/pnet/pnet_injector_test.cpp (addtest pnet_injector_test linking p2p_pnet_upgrader + Boost::Boost.DI + the minimal deps for makeNetworkInjector composition — keep the link set as narrow as_compiles; if the full network graph is unbuildable on MSVC due to the pre-existing soralog/yamux issue, compose a REDUCED injector replicating the same bindings — bind<Upgrader> to PnetUpgraderDecorator vs UpgraderImpl with real UpgraderImpl deps mocked — and assert resolution; document the substitution in the test header comment). Cases: (a) usePrivateNetwork(valid base16 key) + injector → create<transport::Upgrader>() resolves to a PnetUpgraderDecorator (dynamic cast), and create<std::shared_ptr<const Psk>>() yields span().size()==32; (b) usePrivateNetwork(invalid: 31 bytes) → PskValidationError thrown from the module call itself (EXPECT_THROW), and no injector/Host construction attempted; (c) plain injector without the module → Upgrader resolves to plain UpgraderImpl (dynamic cast negative for the decorator) and createOptional/shared_ptr<const Psk> resolves null/absent — public mode, today's behavior (D-08).
  </action>
  <verify>cmake --build build --target pnet_injector_test --config Debug && ctest -C Debug -R pnet_injector_test --output-on-failure (run from build/)</verify>
  <acceptance_criteria>
    - include/libp2p/injector/network_injector.hpp contains struct PskValidationError and template usePrivateNetwork binding BOTH shared_ptr<const Psk> AND Upgrader→PnetUpgraderDecorator with [boost::di::override] in ONE injector (D-07 combined — grep shows both binds in one make_injector call)
    - No default (module-independent) binding of shared_ptr<const Psk> inside the base makeNetworkInjector defaults block (D-08) — or if A2 forced one, it is exactly the documented nullptr minimal default
    - Invalid-key path throws PskValidationError eagerly (test case b asserts EXPECT_THROW at module invocation, before injector assembly)
    - ctest -C Debug -R pnet_injector_test passes: decorator resolution, null-absence, both asserted
  </acceptance_criteria>
  <done>One-line usePrivateNetwork(key) activates private-network mode end-to-end in DI; invalid keys throw before any Host exists (PNET-05); absence leaves public behavior identical (PNET-04/D-08).</done>
</task>

<task type="auto">
  <name>Task 3: DialerImpl nullable-PSK ctor param + public-bootstrap dial refusal + tests</name>
  <files>include/libp2p/network/impl/dialer_impl.hpp, src/network/impl/dialer_impl.cpp, test/libp2p/network/dialer_test.cpp</files>
  <read_first>
    - include/libp2p/network/impl/dialer_impl.hpp (ctor param list — append psk after gater following the Phase 1 pattern; member list)
    - src/network/impl/dialer_impl.cpp (the gater interceptPeerDial refusal block to clone ~lines 24-34: SL_DEBUG + scheduler_->schedule(cb, err) + return)
    - include/libp2p/peer/address_repository.hpp (kBootstrapAddress = /dnsaddr/bootstrap.libp2p.io — the primary match target)
    - test/libp2p/network/dialer_test.cpp (existing fixture: gater mock, ManualSchedulerBackend, drain idiom — extend, don't rebuild)
    - test/libp2p/network/CMakeLists.txt (dialer_test link set — add p2p_pnet)
    - .planning/phases/02-private-network-pnet-psk-protector/02-RESEARCH.md Pattern 4 (refusal semantics + why resolved-address peer-ID matching is needed) and Open Question 3 (transcribe the public peer-ID snapshot at implementation time from bootstrap.libp2p.io dnsaddr records / ipfs docs)
    - .planning/phases/02-private-network-pnet-psk-protector/02-PATTERNS.md §dialer_impl assignment (exact ctor snippet + refusal block to copy)
  </read_first>
  <action>
1. include/libp2p/network/impl/dialer_impl.hpp: append ctor param std::shared_ptr<const security::pnet::Psk> psk = nullptr after gater (D-08: null default = public mode — absence is meaningful), plus member std::shared_ptr<const security::pnet::Psk> psk_; next to gater_. Update the injector-visible construction path implicitly (Boost.DI resolves the new param; when no Psk is bound it must inject nullptr — Task 2 established the resolution behavior).

2. src/network/impl/dialer_impl.cpp, in dial() next to the Phase 1 gater block, add the D-12 refusal: if psk_ != nullptr AND the dial target matches the public-bootstrap set → SL_DEBUG log with peer id + address (D-13), scheduler_->schedule the callback with PnetError::PNET_PUBLIC_BOOTSTRAP_REFUSED, return. Match set: (a) multiaddress strings containing /dnsaddr/bootstrap.libp2p.io (kBootstrapAddress) — match by string containment on the peer-info addresses; (b) a constexpr snapshot (std::array of base58 strings or PeerId literals) of well-known public bootstrap peer IDs transcribed at implementation time from the go-libp2p/ipfs public bootstrap set (research Open Q3; staleness acceptable per A3 — the dnsaddr match is the primary guard). Also update src/network CMake link if p2p_pnet is not already in the dialer target's link set (it will need p2p_pnet for Psk/PnetError).

3. Extend test/libp2p/network/dialer_test.cpp: construct DialerImpl twice — without psk (existing fixture unchanged, plus one new case asserting a bootstrap-address dial PROCEEDS to transport as today) and with a valid psk (fixture gains a psk member built via Psk::fromBase16String of a fixed 64-char hex key). Cases: (a) psk present + dial to a PeerInfo whose address contains /dnsaddr/bootstrap.libp2p.io → callback receives PNET_PUBLIC_BOOTSTRAP_REFUSED after scheduler drain, transport NEVER dialed (mock expectation Times(0)); (b) psk present + dial whose peer id is in the snapshot constexpr set (add one snapshot ID to the match list from the transcribed set) → refused; (c) psk present + ordinary private peer → dial proceeds exactly as the existing happy-path tests; (d) psk null + bootstrap-address dial → proceeds (public mode untouched, D-08). Add p2p_pnet to dialer_test's target_link_libraries.
  </action>
  <verify>cmake --build build --target dialer_test --config Debug && ctest -C Debug -R dialer_test --output-on-failure (run from build/)</verify>
  <acceptance_criteria>
    - dialer_impl.hpp ctor declares std::shared_ptr<const security::pnet::Psk> psk = nullptr (nullable, defaulted)
    - dialer_impl.cpp refusal block contains SL_DEBUG, scheduler_->schedule, and PNET_PUBLIC_BOOTSTRAP_REFUSED, structurally mirroring the gater refusal (grep finds all three within the block)
    - The public-bootstrap match set includes kBootstrapAddress-style dnsaddr containment AND a constexpr peer-ID list (grep finds the constexpr/static list)
    - No Psk key bytes appear in any SL_*/log call in dialer_impl.cpp (log formats peer id + address only)
    - ctest -C Debug -R dialer_test passes including the four new cases; transport mock Times(0) on refused dials
  </acceptance_criteria>
  <done>Public-bootstrap dials are silently filtered when a PSK is configured (BOOT-01, D-11/D-12/D-13); public-mode dials byte-identical to today; failures observable only via SL_DEBUG.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integrator configuration → DI graph | usePrivateNetwork input (key string) is the entry boundary; eager validation is the control |
| Private-network node → public DHT infrastructure | Dial targets crossing this boundary leak topology/metadata; dial-time refusal is the control |
| DI graph internals | Boost.DI composition surface (recursion/null-resolution failure modes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Elevation | silent fallback to public mode when pnet misconfigured | mitigate | D-08 no default binding + D-09/D-10 eager throw; injector test case (b) asserts PskValidationError at module call |
| T-02-15 | Information Disclosure | private node dialing public bootstrapers | mitigate | D-12 dial-time refusal (dnsaddr containment + peer-ID snapshot) inside DialerImpl; SL_DEBUG observability (D-13); dialer test cases (a)/(b) |
| T-02-16 | Information Disclosure | relay upgrade paths bypassing PSK wrap | accept | Research A4 / Open Q1: relay streams pass through unwrapped this phase; documented at the decorator; GNUS private nets non-relay by default; revisit if SuperGenius uses circuit relay |
| T-02-17 | Tampering/DoS | Boost.DI recursion or null-resolution breaking graph construction | mitigate | Concrete UpgraderImpl ctor param (Pattern 2) + Wave-0-style DI smoke test gate resolving A1/A2 at compile/test time |
| T-02-18 | Information Disclosure | peer-ID snapshot drift weakening the bootstrap filter | accept | A3: snapshot is advisory; dnsaddr containment is primary; misconfig premise (D-11) means blast radius is small |
| T-02-19 | Information Disclosure | refusal logs echoing key material | mitigate | Log formats peer id + multiaddress only; acceptance criterion greps for psk bytes in log calls |
</threat_model>

<verification>
- cmake --build build --target pnet_upgrader_decorator_test pnet_injector_test dialer_test --config Debug succeeds
- ctest -C Debug -R "pnet_upgrader_decorator_test|pnet_injector_test|dialer_test" passes
- ctest -C Debug -R "xsalsa20_test|psk_test|pnet_protected_connection_test" still passes (phase regression sweep)
- Existing suite spot-check: ctest -C Debug -R upgrader (upgrader_session behavior unchanged — decorator only rebinds when module applied)
</verification>

## Artifacts this phase produces
*(Plan 02-04 contribution; completes the phase-level artifact list with Plans 02-01/02-03)*

- Class: `libp2p::transport::PnetUpgraderDecorator` — Upgrader decorator, ctor `(std::shared_ptr<UpgraderImpl>, std::shared_ptr<const Psk>, std::shared_ptr<Scheduler>)`; wraps `upgradeToSecureOutbound/upgradeToSecureInbound`, passes relay overloads + `upgradeToMuxed` through
- Exception type: `libp2p::injector::PskValidationError` (carries `PnetError`)
- DI module: `usePrivateNetwork(key)` + exception-free `usePrivateNetwork(Psk)` overload — binds `shared_ptr<const Psk>` + rebinds `Upgrader`→`PnetUpgraderDecorator` in one injector (D-07)
- Modified ctor: `DialerImpl(..., gater, psk = nullptr)` + dial-time public-bootstrap refusal emitting `PnetError::PNET_PUBLIC_BOOTSTRAP_REFUSED` via `SL_DEBUG` + scheduler
- Constant: public-bootstrap peer-ID snapshot (`constexpr` list beside the refusal check)
- CMake targets: `p2p_pnet_upgrader` (src/transport/impl/); test targets `pnet_upgrader_decorator_test`, `pnet_injector_test`; `dialer_test` link set extended with `p2p_pnet`
- New files: `include/libp2p/transport/impl/pnet_upgrader_decorator.hpp`, `src/transport/impl/pnet_upgrader_decorator.cpp`, `test/libp2p/security/pnet/{pnet_upgrader_decorator_test.cpp,pnet_injector_test.cpp}`
- Modified files: `include/libp2p/injector/network_injector.hpp`, `include/libp2p/network/impl/dialer_impl.hpp`, `src/network/impl/dialer_impl.cpp`, `test/libp2p/network/dialer_test.cpp`, `test/libp2p/network/CMakeLists.txt`

<success_criteria>
- One-line `usePrivateNetwork(key)` turns on private-network mode; invalid keys throw before any Host exists; absence = public mode identical to today (PNET-04/PNET-05, D-07/D-08/D-09/D-10)
- Both upgrade paths wrap raw connections before multiselect when enabled (PNET-01 complete)
- Public-bootstrap dials silently refused with a Psk present, observable at SL_DEBUG; unaffected without (BOOT-01, D-11/D-12/D-13)
- All six new/extended test targets pass; Phase 1 dialer/upgrader behavior unchanged
</success_criteria>

<output>
Create `.planning/phases/02-private-network-pnet-psk-protector/02-04-SUMMARY.md` when done
</output>
