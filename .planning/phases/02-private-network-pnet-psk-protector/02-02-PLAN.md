---
phase: 02-private-network-pnet-psk-protector
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - include/libp2p/security/pnet/pnet_error.hpp
  - include/libp2p/security/pnet/psk.hpp
  - src/security/pnet/pnet_error.cpp
  - src/security/pnet/psk.cpp
  - src/security/pnet/CMakeLists.txt
  - src/security/CMakeLists.txt
  - test/libp2p/security/pnet/psk_test.cpp
  - test/libp2p/security/pnet/CMakeLists.txt
  - test/libp2p/security/CMakeLists.txt
autonomous: true
requirements: [PNET-02, PNET-04, PNET-05, TEST-02]
must_haves:
  truths:
    - "An existing GNUS go-ipfs swarm.key file text parses as-is into a 32-byte Psk, from both /base16/ and /base64/ framing"
    - "A raw hex or base64 string (no file framing) also parses into a Psk"
    - "Any parse failure — wrong length (31/33 bytes), bad encoding, missing /key/swarm/psk/1.0.0/ header, wrong version — returns an explicit PnetError, never a silently truncated key"
    - "Psk cannot be copied (compile-time enforced) and its key bytes are zeroed on destruction/move"
    - "Psk never exposes its bytes as a std::vector<uint8_t> and no pnet file logs key material"
  artifacts:
    - path: "include/libp2p/security/pnet/pnet_error.hpp"
      provides: "libp2p::security::pnet::PnetError enum, values start at = 1, PNET_-prefixed"
      contains: "PNET_INVALID_PSK_FORMAT"
    - path: "include/libp2p/security/pnet/psk.hpp"
      provides: "Move-only, self-zeroing Psk value type with outcome::result factories"
      contains: "fromSwarmKeyText"
    - path: "test/libp2p/security/pnet/psk_test.cpp"
      provides: "Full D-05 parse-matrix test suite"
      min_lines: 80
  key_links:
    - from: "src/security/pnet/psk.cpp"
      to: "libp2p::common::unhex"
      via: "hex decoding reuse (never hand-rolled)"
      pattern: "common::unhex"
    - from: "src/security/pnet/psk.cpp"
      to: "multibase_codec::decodeBase64"
      via: "base64 decoding reuse"
      pattern: "decodeBase64"
    - from: "src/security/CMakeLists.txt"
      to: "src/security/pnet"
      via: "add_subdirectory(pnet)"
      pattern: "add_subdirectory\\(pnet\\)"
---

<objective>
Create the pnet configuration surface: the `PnetError` enum and the `Psk` value type — dual-format parsing (go-ipfs swarm.key text AND raw hex/base64 strings), exactly-32-byte validation at parse time, and strict key hygiene (move-only, self-zeroing, never logged).

Purpose: PNET-04 (DI-configured PSK — the `Psk` type is what `usePrivateNetwork(key)` in Plan 04 binds) and PNET-05 (invalid PSK → explicit failure — validated at parse time in the factories that Plan 04's module calls eagerly before any Host exists, per D-09/D-10).

Output: `p2p_pnet` leaf library currently containing `PnetError` + `Psk` (Plan 03 will add the protected connection to the same target), plus test target `psk_test`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-private-network-pnet-psk-protector/02-CONTEXT.md
@.planning/phases/02-private-network-pnet-psk-protector/02-RESEARCH.md
@.planning/phases/02-private-network-pnet-psk-protector/02-PATTERNS.md

@include/libp2p/network/connection_gater_error.hpp
@include/libp2p/common/hexutil.hpp
@include/libp2p/multi/multibase_codec/codecs/base64.hpp
@test/libp2p/security/CMakeLists.txt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PnetError enum + Psk value type with dual-format factories</name>
  <files>include/libp2p/security/pnet/pnet_error.hpp, include/libp2p/security/pnet/psk.hpp, src/security/pnet/pnet_error.cpp, src/security/pnet/psk.cpp, src/security/pnet/CMakeLists.txt, src/security/CMakeLists.txt</files>
  <read_first>
    - include/libp2p/network/connection_gater_error.hpp + src/network/connection_gater_error.cpp (exact error-enum/category pattern from Phase 1 — copy structure)
    - include/libp2p/common/hexutil.hpp (common::unhex signature used for hex decoding reuse)
    - include/libp2p/multi/multibase_codec/codecs/base64.hpp (decodeBase64 signature)
    - src/security/CMakeLists.txt (where add_subdirectory(pnet) goes)
    - .planning/phases/02-private-network-pnet-psk-protector/02-PATTERNS.md §psk assignment (full Psk class-shape excerpt — the concrete form to implement)
    - .planning/phases/02-private-network-pnet-psk-protector/02-RESEARCH.md §Code Examples swarm.key format block and Pitfalls 5/8
  </read_first>
  <behavior>
    - Test: fromSwarmKeyText with /key/swarm/psk/1.0.0/ + /base16/ + 64 hex chars → success, span() is exactly 32 bytes matching the decoded key
    - Test: fromSwarmKeyText with /base64/ framing (base64 of 32 bytes) → success
    - Test: fromBase16String("...") and fromBase64String("...") raw (no framing) → success
    - Test: fromRawBytes with 31 bytes → PNET_INVALID_PSK_LENGTH; 33 bytes → PNET_INVALID_PSK_LENGTH; 32 bytes → success
    - Test: 64-char non-hex payload → PNET_INVALID_PSK_FORMAT; missing /key/swarm/psk/1.0.0/ header → PNET_INVALID_PSK_FORMAT; wrong version string (/key/swarm/psk/2.0.0/ or similar) → PNET_INVALID_PSK_FORMAT; trailing "\n" tolerated (success)
    - Test: static_assert(!std::is_copy_constructible_v<Psk>) — copy is deleted at compile time
    - Test: PNET_PUBLIC_BOOTSTRAP_REFUSED and every declared enum value map to a non-empty message string containing "pnet:"
  </behavior>
  <action>
Create the pnet config surface per D-05/D-06, namespace libp2p::security::pnet:

1. `pnet_error.hpp` (guard LIBP2P_SECURITY_PNET_PNET_ERROR_HPP): `enum class PnetError` with SCREAMING_SNAKE values starting at = 1 and ///< Doxygen trailing comments — minimally: PNET_INVALID_PSK_FORMAT, PNET_INVALID_PSK_LENGTH, PNET_NONCE_GENERATION_FAILED, PNET_NONCE_READ_FAILED, PNET_ENCRYPT_FAILED, PNET_PUBLIC_BOOTSTRAP_REFUSED (these last ones consumed by Plans 03/04). OUTCOME_HPP_DECLARE_ERROR at file scope; pnet_error.cpp registers via OUTCOME_CPP_DEFINE_CATEGORY with message strings carrying "pnet:" attribution ("pnet: invalid pre-shared key length" etc. — Phase 1's unmistakable-attribution convention).

2. `psk.hpp` (guard LIBP2P_SECURITY_PNET_PSK_HPP): class Psk per the pattern-map shape — std::array<uint8_t, 32> key_ member; static factories fromSwarmKeyText(std::string_view), fromRawBytes(gsl::span<const uint8_t>), fromBase16String(std::string_view), fromBase64String(std::string_view), all returning outcome::result<Psk>; copy ctor/assignment deleted; move ctor/assignment noexcept that move-and-OPENSSL_cleanse the source; destructor calls OPENSSL_cleanse on key_; accessor gsl::span<const uint8_t> span() const noexcept returning exactly 32 bytes. Private default ctor so only factories construct. NO logger in this file at all (D-06 — key bytes must never be loggable; RESEARCH Pitfall 8).

3. `psk.cpp` factory semantics (D-05):
   - fromSwarmKeyText: strictly parse the framing lines — /key/swarm/psk/1.0.0/ (exact version match) + codec line /base16/ or /base64/ (/bin/ → PNET_INVALID_PSK_FORMAT per RESEARCH A5 — parser rejects it, documented in the header comment) + payload line. Tolerate trailing whitespace/newlines (go-ipfs files end with \n). Decode payload via common::unhex (case-insensitive) or multibase_codec::decodeBase64 — NEVER hand-rolled decoding.
   - fromBase16String: common::unhex directly. fromBase64String: decodeBase64 directly. fromRawBytes: direct.
   - Every decoded result then checks decoded size == 32 exactly → else PNET_INVALID_PSK_LENGTH (31B and 33B both rejected; never truncate, never accept-shorter).
   - Any decode-framework failure → PNET_INVALID_PSK_FORMAT.

4. CMake: src/security/pnet/CMakeLists.txt with libp2p_add_library(p2p_pnet pnet_error.cpp psk.cpp), linking Boost::boost, OpenSSL::Crypto (for OPENSSL_cleanse), and the decode-utility targets that hexutil/base64 actually live in (check where common::unhex and multibase_codec::decodeBase64 are defined and link those — likely p2p_common and p2p_multibase_codec; keep minimal, no yamux/network targets, RESEARCH Pitfall 6). Add add_subdirectory(pnet) to src/security/CMakeLists.txt (alphabetical, after noise and before plaintext).
  </action>
  <verify>cmake --build build --target p2p_pnet --config Debug</verify>
  <acceptance_criteria>
    - include/libp2p/security/pnet/psk.hpp contains fromSwarmKeyText, fromRawBytes, fromBase16String, fromBase64String and "Psk(const Psk &) = delete"
    - src/security/pnet/psk.cpp contains calls to common::unhex (or unhex via using-declaration) and a base64 decode — grep finds no hand-rolled hex/base64 loop (pattern: a for-loop indexing a 16-char alphabet table)
    - src/security/pnet/psk.cpp destructor/move contains OPENSSL_cleanse
    - No file under include/libp2p/security/pnet/ or src/security/pnet/ matches SL_TRACE|SL_DEBUG|SL_INFO|SL_WARN|SL_ERROR referencing key bytes (D-06 no-logging rule; psk.hpp/.cpp contain zero log statements)
    - grep -c "PNET_INVALID_PSK_LENGTH" in psk.cpp >= 1 and size-check happens after decode, comparing against 32
    - src/security/CMakeLists.txt contains add_subdirectory(pnet)
    - cmake --build build --target p2p_pnet --config Debug exits 0
  </acceptance_criteria>
  <done>p2p_pnet leaf library builds with PnetError + move-only self-zeroing Psk; all D-05 parse paths explicit-outcome, decode-reuse only, zero logging.</done>
</task>

<task type="auto">
  <name>Task 2: psk_test parse matrix + hygiene checks</name>
  <files>test/libp2p/security/pnet/psk_test.cpp, test/libp2p/security/pnet/CMakeLists.txt, test/libp2p/security/CMakeLists.txt</files>
  <read_first>
    - test/libp2p/security/CMakeLists.txt (addtest pattern; add add_subdirectory(pnet) here)
    - test/libp2p/security/pnet/psk_test.cpp (the sensor file under creation — keep test style consistent with e.g. test/libp2p/security/plaintext_adaptor_test.cpp conventions)
    - test/libp2p/security/plaintext_adaptor_test.cpp (fixture/assertion style analog)
    - include/libp2p/security/pnet/psk.hpp (API under test)
    - .planning/phases/02-private-network-pnet-psk-protector/02-RESEARCH.md §Test Matrix row psk_test.cpp (the exact case list)
  </read_first>
  <action>
Create test/libp2p/security/pnet/psk_test.cpp + test/libp2p/security/pnet/CMakeLists.txt; register add_subdirectory(pnet) in test/libp2p/security/CMakeLists.txt. Target: addtest(psk_test psk_test.cpp) linking p2p_pnet (+ p2p_literals/p2p_testutil only if actually needed).

Cases (full D-05/RESEARCH matrix):
1. SwarmKeyBase16Valid / SwarmKeyBase64Valid: complete file text including /key/swarm/psk/1.0.0/ header, codec line, payload, trailing \n → success; assert span().size() == 32 and bytes match the decoded expectation.
2. RawConstructors: fromBase16String/fromBase64String/fromRawBytes all succeed for a fixed 32-byte key and produce identical span() bytes.
3. Rejects (each asserts the exact error code): 31-byte raw → PNET_INVALID_PSK_LENGTH; 33-byte raw → PNET_INVALID_PSK_LENGTH; 62/66-char hex (decodes to 31/33 bytes) → PNET_INVALID_PSK_LENGTH; 64-char non-hex payload → PNET_INVALID_PSK_FORMAT; missing header → PNET_INVALID_PSK_FORMAT; wrong version /key/swarm/psk/2.0.0/ → PNET_INVALID_PSK_FORMAT.
4. MoveOnly: static_assert(!std::is_copy_constructible_v<Psk>) and static_assert(std::is_move_constructible_v<Psk>) at namespace scope in the test; move leaves the source usable-but-clean (can't read its bytes via public API post-move — just assert moved-to object span() still yields the original 32 bytes).
5. ZeroingObservable: destruct a Psk whose key buffer was heap-adjacent to a sentinel — practical form: construct via factory, copy span() bytes into a local vector, destroy the Psk, assert your local copy is unchanged while re-reading any externally retained alias is impossible by construction (the real guarantee is OPENSSL_cleanse in the dtor — verified by acceptance-criteria grep on psk.cpp; keep the test as API-shape asserts, not UB attempts to read freed bytes).
6. ErrorMessages: every PnetError enumerator formats a message containing "pnet:" (outcome error().message()).
  </action>
  <verify>cmake --build build --target psk_test --config Debug && ctest -C Debug -R psk_test --output-on-failure (run from build/)</verify>
  <acceptance_criteria>
    - ctest -C Debug -R psk_test passes
    - psk_test.cpp contains the static_assert(!std::is_copy_constructible_v<Psk>) compile-time check
    - psk_test.cpp rejects test cells assert both PNET_INVALID_PSK_LENGTH and PNET_INVALID_PSK_FORMAT distinctly (grep finds both enum names)
    - test/libp2p/security/CMakeLists.txt contains add_subdirectory(pnet); test/libp2p/security/pnet/CMakeLists.txt contains addtest(psk_test and links p2p_pnet
  </acceptance_criteria>
  <done>psk_test passes under ctest -C Debug covering the complete parse matrix, hygiene compile-time checks, and "pnet:"-attributed error messages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integrator config (swarm.key file / strings) → Psk | Untrusted-by-default configuration input crosses here; strict validation is the gate |
| Psk key material ↔ memory | Secret at rest in process memory; hygiene rules D-06 are the control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | psk.cpp parsing (silent truncation/acceptance of malformed keys) | mitigate | D-05 exactly-32-byte post-decode check; explicit PnetError for every reject cell; full matrix in psk_test |
| T-02-06 | Information Disclosure | key bytes in logs or lingering memory | mitigate | D-06: move-only type, OPENSSL_cleanse dtor/move, zero log statements in pnet files (grep-enforced acceptance criterion) |
| T-02-07 | Elevation | half-configured private-network mode (invalid key silently accepted → public fallback) | mitigate | Factories return explicit errors that Plan 04's usePrivateNetwork turns into eager construction failure (D-09/D-10; PNET-05) |
</threat_model>

<verification>
- cmake --build build --target psk_test --config Debug succeeds
- ctest -C Debug -R psk_test passes
- Zero log statements in pnet header/source files (D-06 grep check)
</verification>

## Artifacts this phase produces
*(Plan 02-02 contribution)*

- Namespace: `libp2p::security::pnet`
- Enum: `PnetError` — values `PNET_INVALID_PSK_FORMAT`, `PNET_INVALID_PSK_LENGTH`, `PNET_NONCE_GENERATION_FAILED`, `PNET_NONCE_READ_FAILED`, `PNET_ENCRYPT_FAILED`, `PNET_PUBLIC_BOOTSTRAP_REFUSED` (start = 1)
- Class: `Psk` — factories `fromSwarmKeyText`, `fromRawBytes`, `fromBase16String`, `fromBase64String`; `span()` accessor; move-only, self-zeroing
- CMake targets: `p2p_pnet` (src/security/pnet/ — currently error+psk; Plan 03 adds connection files to this same target), test target `psk_test`
- New files: `include/libp2p/security/pnet/{pnet_error.hpp,psk.hpp}`, `src/security/pnet/{pnet_error.cpp,psk.cpp,CMakeLists.txt}`, `test/libp2p/security/pnet/{psk_test.cpp,CMakeLists.txt}`

<success_criteria>
- Existing-format swarm.key text (base16 and base64) parses as-is (D-05, GNUS compatibility)
- Every malformed input rejected explicitly with a distinct PnetError — no silent acceptance path exists
- Psk hygiene enforced: copy deleted at compile time, dtor/move zeros via OPENSSL_cleanse, no logging anywhere in pnet files (D-06)
- p2p_pnet builds without yamux/network-family dependencies (Pitfall 6 constraints respected for local MSVC verification)
</success_criteria>

<output>
Create `.planning/phases/02-private-network-pnet-psk-protector/02-02-SUMMARY.md` when done
</output>
