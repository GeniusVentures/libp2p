---
phase: 01-connection-gater-interface-wiring
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - include/libp2p/network/impl/dialer_impl.hpp
  - src/network/impl/dialer_impl.cpp
  - src/network/impl/CMakeLists.txt
  - test/libp2p/network/dialer_test.cpp
autonomous: true
requirements: [GATE-03, TEST-01]

must_haves:
  truths:
    - "A gater configured to reject interceptPeerDial prevents DialerImpl::dial() from ever calling TransportManager::findBest() or Transport::dial() for that peer (roadmap criterion 2)"
    - "A gater configured to reject interceptAddrDial for a specific address prevents Transport::dial() from ever being invoked for that address, in both DialerImpl::rotate()'s relay and non-relay branches and in DialerImpl::rotateHolepunch()'s loop (roadmap criterion 2 + resolved holepunch decision)"
    - "Every interceptPeerDial/interceptAddrDial rejection is logged via SL_DEBUG including the peer id and, where available, the address, and the caller-visible dial callback is always delivered via scheduler_->schedule(...), never invoked synchronously inline"
  artifacts:
    - "DialerImpl 6-arg constructor (adds gater)"
  key_links:
    - "DialerImpl::gater_ (constructed via Boost.DI, resolving to PermissiveConnectionGater by default per Plan 01-01) -> interceptPeerDial()/interceptAddrDial() calls in dial()/rotate()/rotateHolepunch()"
    - "Gater-rejected address in rotate() -> DialCtx::result (existing field) -> completeDial() surfaces the ConnectionGaterError once all addresses are exhausted"
---

<objective>
Wire `ConnectionGater` into `DialerImpl`'s peer-dial and address-dial paths: `interceptPeerDial` gates `dial()` before any connection reuse/lookup happens, and `interceptAddrDial` gates every `Transport::dial(...)` call site — `rotate()`'s relay branch, `rotate()`'s non-relay branch, and `rotateHolepunch()`'s loop (the previously-silent holepunch bypass called out in RESEARCH.md Pitfall 3 and locked by the resolved open question).

Purpose: Satisfy GATE-03 for the Dialer stage and TEST-01 for both dial-stage hooks.
Output: `DialerImpl` consults the gater before opening any socket to a peer/address; 2 new `DialerTest` cases proving rejection blocks the transport layer entirely.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-connection-gater-interface-wiring/01-CONTEXT.md
@.planning/phases/01-connection-gater-interface-wiring/01-RESEARCH.md
@.planning/phases/01-connection-gater-interface-wiring/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire ConnectionGater into DialerImpl's dial/rotate/rotateHolepunch paths</name>
  <files>include/libp2p/network/impl/dialer_impl.hpp, src/network/impl/dialer_impl.cpp, src/network/impl/CMakeLists.txt</files>
  <read_first>
    include/libp2p/network/impl/dialer_impl.hpp (full — existing 5-arg constructor, DialCtx struct with its `boost::optional<DialResult> result` field);
    src/network/impl/dialer_impl.cpp (full — dial() lines ~18-95, rotate() lines ~97-225 with its relay branch ~171-206 and non-relay branch ~207-223, rotateHolepunch() lines ~227-288, the existing empty-peer-id / no-transport-found scheduler_->schedule(...) retry idioms);
    include/libp2p/network/connection_gater.hpp and include/libp2p/network/connection_gater_error.hpp (Plan 01-01 outputs — hook signatures and error enum);
    src/network/impl/CMakeLists.txt (existing p2p_dialer target to extend)
  </read_first>
  <behavior>
    - interceptPeerDial rejection: dial() must schedule cb(GATER_REJECTED_PEER_DIAL) and never call cmgr_->getBestConnectionForPeer / tmgr_->findBest / transport->dial for that peer.
    - interceptAddrDial rejection (rotate, non-relay branch): tr->dial(...) must never be called for the rejected address; ctx.result is set to the gater error so, if no other address succeeds, completeDial() surfaces GATER_REJECTED_ADDR_DIAL.
    - interceptAddrDial rejection (rotate, relay branch): identical behavior, gated using the outer peer_id (the ultimate dial target), not the relay hop's peer_id_actual.
    - interceptAddrDial rejection (rotateHolepunch loop): tr->dial(...) is skipped for that address; indctx.dialled is left false; the loop continues to the next address (holepunch completion-tracking gaps are pre-existing and out of scope).
    - Every rejection is logged via SL_DEBUG with peer id (and address, where applicable) before scheduling/skipping.
  </behavior>
  <action>
    In include/libp2p/network/impl/dialer_impl.hpp: add `#include <libp2p/network/connection_gater.hpp>`; append a 6th constructor parameter `std::shared_ptr<ConnectionGater> gater` after the existing `scheduler` parameter; add a matching private member `std::shared_ptr<ConnectionGater> gater_;` after `scheduler_`.

    In src/network/impl/dialer_impl.cpp: update the constructor definition to accept `gater` and initialize `gater_(std::move(gater))`, and add `BOOST_ASSERT(gater_ != nullptr);` alongside the existing 5 asserts in the constructor body, matching this file's existing assert-block style exactly.

    In `dial()`: immediately after the existing `if (p.id.toBase58().size() == 0) { ... return; }` block and before the `SL_TRACE(log_, "Dialing to {} ...")` line, insert an `interceptPeerDial(p.id)` check. On rejection: `SL_DEBUG(log_, "gater rejected peer dial to {}: {}", p.id.toBase58(), gated.error().message());` then `scheduler_->schedule([cb{std::move(cb)}, err{gated.error()}] { cb(err); });` then `return;` — placed before the connection-reuse check (`cmgr_->getBestConnectionForPeer`) and the in-progress-dial check, so a rejection blocks the peer entirely, matching "before Dialer attempts to dial peer p at all."

    In `rotate()`'s non-relay branch (the `else { if (auto tr = tmgr_->findBest(addr); nullptr != tr) { ctx.dialled = true; ... tr->dial(peer_id, addr, dial_handler, ctx.timeout, ctx.source_addresses); } else { ...reschedule... } }` block): insert `interceptAddrDial(peer_id, addr)` immediately before the `tr->dial(...)` call, nested inside the `if (auto tr = tmgr_->findBest(addr); nullptr != tr)` branch. On rejection: `SL_DEBUG(log_, "gater rejected addr dial to {} at {}: {}", peer_id.toBase58(), addr.getStringAddress(), gated.error().message());`, set `ctx.result = outcome::failure(gated.error());` (reusing the existing DialCtx::result propagation field so completeDial() surfaces the gater-specific error once addresses are exhausted, via the pre-existing `ctx.addresses.empty() && ctx.result.has_value()` branch at the top of rotate()), then `scheduler_->schedule([wp{weak_from_this()}, peer_id] { if (auto self = wp.lock()) { self->rotate(peer_id); } });` and `return;` without calling `tr->dial(...)` and without setting `ctx.dialled = true` — mirroring the existing "no transport found" else-branch's reschedule-and-retry shape exactly.

    In `rotate()`'s relay branch (inside `if (addr.hasCircuitRelay()) { ... if (auto tr = tmgr_->findBest(addr); nullptr != tr && peer_id_actual) { ... else { tr->dial(peer_id_actual.value(), addr, dial_handler, ctx.timeout, ctx.source_addresses, ctx.holepunch, ctx.holepunchserver); } } ... }`): insert the identical `interceptAddrDial(peer_id, addr)` check — using the outer `peer_id` (the peer the caller asked to reach), not `peer_id_actual` (the intermediate relay hop) — immediately before that `tr->dial(...)` call, with the same rejection handling (log, set `ctx.result`, reschedule rotate(), return, skip the dial).

    In `rotateHolepunch()`'s address loop (`for (auto it = indctx.addresses.begin(); ...) { auto addr = *it; indctx.tried_addresses.insert(addr); it = indctx.addresses.erase(it); if (auto tr = tmgr_->findBest(addr); nullptr != tr) { indctx.dialled = true; ... tr->dial(peer_id, addr, dial_handler, indctx.timeout, indctx.source_addresses); } }`): insert `interceptAddrDial(peer_id, addr)` immediately before that `tr->dial(...)` call, nested inside the `if (auto tr = tmgr_->findBest(addr); nullptr != tr)` branch. On rejection: `SL_DEBUG(log_, "gater rejected holepunch addr dial to {} at {}: {}", peer_id.toBase58(), addr.getStringAddress(), gated.error().message());` and skip the `tr->dial(...)` call for that address (do not set `indctx.dialled = true`), letting the for loop continue to the next address — per the resolved open-question decision, rotateHolepunch() must be gated identically to rotate()'s non-relay branch, closing the previously-silent holepunch bypass.

    In src/network/impl/CMakeLists.txt: add `p2p_connection_gater` to `p2p_dialer`'s `target_link_libraries(...)` list (needed for the `ConnectionGaterError` category symbol referenced by the new `.error().message()` calls).
  </action>
  <acceptance_criteria>
    DialerImpl's constructor has 6 parameters, the last named gater; gater_ member exists with BOOST_ASSERT(gater_ != nullptr) in the constructor body;
    interceptPeerDial is called exactly once per dial() invocation, before cmgr_->getBestConnectionForPeer and before the dialing_peers_/dialing_holepunches_ lookups;
    interceptAddrDial is called immediately before all 3 tr->dial(...) call sites (rotate() relay branch, rotate() non-relay branch, rotateHolepunch() loop) — `grep -c 'interceptAddrDial(' src/network/impl/dialer_impl.cpp` returns 3, `grep -c 'interceptPeerDial(' src/network/impl/dialer_impl.cpp` returns 1;
    on any interceptAddrDial rejection, tr->dial(...) is never invoked for that address and ctx.dialled/indctx.dialled is left false;
    every rejection path logs via SL_DEBUG with the peer id and, where applicable, the address.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build --target dialer_test</automated>
  </verify>
  <done>DialerImpl compiles with the 6-arg constructor and all 4 gater call sites (1 interceptPeerDial + 3 interceptAddrDial) wired exactly as specified, including the previously-unwired rotateHolepunch() path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TEST-01 dialer gater unit tests</name>
  <files>test/libp2p/network/dialer_test.cpp</files>
  <read_first>
    test/libp2p/network/dialer_test.cpp (full — existing DialerTest fixture and its 8 TEST_F cases, the scheduler_backend pump idiom);
    test/mock/libp2p/network/connection_gater_mock.hpp (Plan 01-01 output);
    include/libp2p/network/connection_gater_error.hpp
  </read_first>
  <behavior>
    - All 8 pre-existing TEST_F(DialerTest, ...) cases keep passing unmodified once `gater` defaults to permissive via ON_CALL.
    - New test DialRejectedByPeerDialGater: interceptPeerDial rejects -> tmgr_->findBest is never called -> callback receives GATER_REJECTED_PEER_DIAL.
    - New test DialRejectedByAddrDialGater: interceptAddrDial rejects for the only address -> transport->dial is never called -> callback eventually receives GATER_REJECTED_ADDR_DIAL (via ctx.result propagation once addresses are exhausted).
  </behavior>
  <action>
    Add `#include "mock/libp2p/network/connection_gater_mock.hpp"` and `#include <libp2p/network/connection_gater_error.hpp>` to dialer_test.cpp's include block. In the DialerTest fixture, add `std::shared_ptr<ConnectionGaterMock> gater = std::make_shared<ConnectionGaterMock>();`. In SetUp(), before constructing `dialer`, add `ON_CALL(*gater, interceptPeerDial(_)).WillByDefault(Return(outcome::success()));` and `ON_CALL(*gater, interceptAddrDial(_, _)).WillByDefault(Return(outcome::success()));`, then update the `dialer = std::make_shared<DialerImpl>(proto_muxer, tmgr, cmgr, listener, scheduler, gater);` call to pass `gater` as the 6th argument — this keeps all 8 pre-existing tests passing unmodified since the default is permissive.

    Add `TEST_F(DialerTest, DialRejectedByPeerDialGater)`: `EXPECT_CALL(*gater, interceptPeerDial(pinfo.id)).WillOnce(Return(ConnectionGaterError::GATER_REJECTED_PEER_DIAL));`, `EXPECT_CALL(*cmgr, getBestConnectionForPeer(pinfo.id)).Times(0);`, `EXPECT_CALL(*tmgr, findBest(_)).Times(0);` — dial `pinfo`, pump `scheduler_backend` exactly like the existing tests (`while (!scheduler_backend->empty()) { scheduler_backend->shift(std::chrono::milliseconds(1)); }`), assert the callback's error equals `static_cast<int>(ConnectionGaterError::GATER_REJECTED_PEER_DIAL)` via `EXPECT_OUTCOME_FALSE`/`EXPECT_EQ`, and `ASSERT_TRUE(executed);`.

    Add `TEST_F(DialerTest, DialRejectedByAddrDialGater)`: `EXPECT_CALL(*cmgr, getBestConnectionForPeer(pinfo.id)).WillOnce(Return(nullptr));`, `EXPECT_CALL(*gater, interceptAddrDial(pinfo.id, ma1)).WillOnce(Return(ConnectionGaterError::GATER_REJECTED_ADDR_DIAL));`, `EXPECT_CALL(*tmgr, findBest(ma1)).WillOnce(Return(transport));`, `EXPECT_CALL(*transport, dial(_, _, _, _)).Times(0);` — dial the single-address `pinfo`, pump the scheduler the same way, assert the callback's error equals `static_cast<int>(ConnectionGaterError::GATER_REJECTED_ADDR_DIAL)`, and `ASSERT_TRUE(executed);`.
  </action>
  <acceptance_criteria>
    All 8 pre-existing DialerTest cases pass unmodified in outcome after the fixture change;
    DialRejectedByPeerDialGater asserts tmgr_->findBest is never called;
    DialRejectedByAddrDialGater asserts transport->dial is never called and the propagated error equals GATER_REJECTED_ADDR_DIAL;
    `ctest --test-dir build -R DialerTest` reports 10/10 passing.
  </acceptance_criteria>
  <verify>
    <automated>cmake -S . -B build &amp;&amp; cmake --build build --target dialer_test &amp;&amp; ctest --test-dir build -R DialerTest --output-on-failure</automated>
  </verify>
  <done>10 DialerTest cases pass: the 8 pre-existing ones unmodified, plus the 2 new gater-rejection cases proving the transport layer is never reached on rejection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| DialerImpl -> TransportManager/Transport | Untrusted target: any peer/address the caller supplies; the gater is the last check before a socket is ever opened |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-01-04 | Elevation of Privilege | DialerImpl::rotateHolepunch() | high | mitigate | Previously-silent holepunch dial-loop bypass (RESEARCH.md Pitfall 3) closed by wiring interceptAddrDial into rotateHolepunch()'s loop identically to rotate() — Task 1 |
| T-01-05 | Tampering / DoS | DialerImpl callback delivery on rejection | medium | mitigate | Every gater-rejection-triggered callback (cb/dial_handler retry) is delivered via scheduler_->schedule(...), never invoked synchronously inline, avoiding the reentrancy bug class documented in .planning/codebase/CONCERNS.md (TODO(107) sites) |
| T-01-06 | Repudiation | Rejection observability | low | mitigate | D-06: every interceptPeerDial/interceptAddrDial rejection is logged via SL_DEBUG with peer id and address |
</threat_model>

<verification>
- `cmake --build build --target dialer_test` compiles cleanly with the new 6-arg DialerImpl constructor.
- `ctest --test-dir build -R DialerTest --output-on-failure` — all 10 cases pass.
- `grep -c 'interceptAddrDial(' src/network/impl/dialer_impl.cpp` == 3; `grep -c 'interceptPeerDial(' src/network/impl/dialer_impl.cpp` == 1.
</verification>

<success_criteria>
GATE-03 is satisfied for the Dialer stage (peer dial + address dial, including the holepunch loop), and TEST-01 has automated coverage for both hooks proving the transport layer is never reached on rejection.
</success_criteria>

## Artifacts this phase produces (this plan)

- `DialerImpl` constructor: adds trailing `std::shared_ptr<network::ConnectionGater> gater` parameter and `gater_` member
- `DialerImpl::dial()` — `interceptPeerDial(p.id)` gate, scheduled rejection callback
- `DialerImpl::rotate()` — `interceptAddrDial(peer_id, addr)` gate in both the relay and non-relay branches
- `DialerImpl::rotateHolepunch()` — `interceptAddrDial(peer_id, addr)` gate (closes the previously-unwired holepunch bypass)
- `TEST_F(DialerTest, DialRejectedByPeerDialGater)`, `TEST_F(DialerTest, DialRejectedByAddrDialGater)`

<output>
Create `.planning/phases/01-connection-gater-interface-wiring/01-02-SUMMARY.md` when done
</output>
